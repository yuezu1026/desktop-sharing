---
description: 检查 docs/design/high 的界面时，用 Playwright 打开渲染后的页面，不要只读源码。
globs: docs/design/high/**/*.{html,css}
alwaysApply: false
---

# 高保真页用 Playwright 检查

改完或评审这些页面，先用 Playwright 打开渲染结果，再下结论。读 HTML / CSS 不能代替。

- 不把 Playwright 装进本仓库：不要在仓库根 `npm install`，不要新增 `package.json`。用 `npx --yes playwright`，脚本和截图放临时目录。
- 用 Node 拼 `file://` 地址。不要把中文文件名交给 PowerShell 5.1 拼进命令行。
- 浏览器未装时，只在临时目录执行 `npx playwright install chromium`。
- 至少核对：横向溢出、会话 16:9 黑边、购买按钮不是青绿、手机可点高度 44px、画布内没有开发说明。
- 线框门禁仍走 `docs/design/wireframe/tools` 的 node 脚本，不要用 Playwright 替换。
- 截图和浏览器缓存不提交。

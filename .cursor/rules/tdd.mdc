---
description: 后续开发按 TDD：先失败用例，再实现，再重构。协议与账本逻辑必须有局部测试。
alwaysApply: true
---

# TDD（已拍板）

用户已确认后续按测试驱动开发。与「不要随便生成全局测试」的关系：**允许且要求**为本刀相关的 crate / 模块写**窄、局部**测试；不要为整仓造冒烟大套件，也不要为 Win32 窗体 / DXGI / SendInput 硬造 fragile UI 测试。

## 流程（每一刀）

1. **红**：先写一个会失败的用例，钉住本刀要守住的行为。
2. **绿**：用最少实现让用例通过。
3. **重构**：在绿的前提下整理，不扩范围。
4. 收口前跑通本刀相关测试，再跑线框两道门禁并提交推送。

## 优先用 TDD 的面

- `crates/session-core`、`direct-client`、`relay-client`、`signal-client` 的纯协议与字节布局
- `services/control-plane` 的账本、直连事件、`inputRevoked`、额度裁决
- 可纯函数化的编码辅助（码流封装、分片重组），不是 GPU/COM 句柄本身

## 不优先 / 用手测的面

- 桌面 Win32 窗口几何与托盘菜单
- DXGI / Media Foundation / SendInput 的设备相关路径（可对**输入输出契约**单测，设备调用用手测或极少集成烟测）
- 真机 NAT / 打洞（分桶上报仍可用固定输入测）

## 写法

- Rust：`#[cfg(test)]` 放在同一文件或同 crate 的 `tests` 模块；用例名用中文或英文均可，断言语义要可读。
- TypeScript：本刀改到的服务用现有跑法加最小用例，不新开全仓测试框架 unless 本刀需要。
- 禁止为了凑数测实现细节；测可观察行为（编解码往返、错误码、状态迁移）。

# 账号与管理系统设计 · 附录：一句话速查

> 📦 由 `账号与管理系统设计.md` 文末附录**原样外置**。

> 📦 **来源**：`docs/账号与管理系统设计.md` 原 L594–650 · 外置于 2026-09-15 · `tools/archive-split.mjs`
> 🔎 正文对应位置仍保留 `§` 编号与标题锚点，可直接在本文件内检索定位。

---

## 附：一句话速查

```
范围      A 运营后台 + B 账号生命周期 + D 个人中心；C 企业级 = v2 §7 明确不做
铁律一    🔴 涉及资金/权限的操作必须走工具 + 留审计，不得直接改库
铁律二    🔑 能复用现成工具的绝不自研（退款/对账/发票→渠道后台；看板→Grafana）
P0 工具   🔑 不是"后台"，是 3 查 + 3 动作：查用户 / 查额度明细 / 查订单
          调额度（补偿）· 封禁解封加白 · 人工退款（跳渠道后台）
后台本质  🔴 已拍板政策的执行体：§5.6 主动补偿 · §9.3 家庭互助白名单 · D11 七天可退
          ⇒ 没有工具 = 承诺落空 = 客诉
优先级    🔴 B > A：看不了后台只是我们难受；解不了绑是用户用不了
解绑      🔴 两种语义必须分开：① 取消授权（设备还在）② 移除设备（清授权）
          换机是隐私事故高发场景 ⇒ 需"一键解绑全部" + 90 天授权复核
设备数    ✅ D15：两档都给 150 台（不作付费点：无边际成本 + 客诉成本高）⇒ 免费给足、压缩对手空间
           ⚠️ 只能作"免费版优势行"，不得当主标语（= 对手 ¥158/年 专业档位）
注销      🔴 合规硬项（个保法删除权 + App Store 5.1.1(v)），App 内必须可达
          ⚠️ 注销 ≠ 取消订阅（iOS 由 Apple 管，不取消会继续扣费）
          ✅ D16：日志留存 6 个月（≥30 天冷静期；注销后匿名化：删身份数据 + 去标识保留流水）
实名      🔴 延迟实名：不触发风控/不用额度/不跨账号连接前不要求（前置会掉转化）
成本参数  ✅ D18：TURN 报价可后台管（仅负责人可改 · 财务只读 · 客服不可见）
          🔴 改参数 = 新增版本；派生的额度/打平 MAU 必须系统算（不得手填）
          ⚠️ 待填参数（TURN 报价）只阻塞对外口径，不阻塞开发（成本测算表 §3.4）
红线      被控端必须能一次点击断开，不得挽留式二次确认（隐私主体）
权限      P0 至少分「客服（只读+小额补偿）」与「管理员」两个角色
内部账号  🔴 邮箱 + 密码 + 强制 TOTP，不复用手机号；不自助注册；2FA 不可跳过不记住设备
          离职 = 停用而非删除（审计不可删）；改密后所有内部台会话失效（§5.7 / [D24]）
支付状态  🔴 订单四态：PENDING / PAID / CONFIRMING / CLOSED（§4.6 · [D25]）
          🔴 幂等键 = 订单号；权益发放以**服务端确认收款**为唯一触发（不信客户端返回值）
          🔴 CONFIRMING 必须能自动落定（不得变永久态）；扣款失败 ≠ 终止（重试 → 7 天宽限）
发票抬头  🔴 订单确认时填一次 → 落账号级发票档案 → 账单页直接带出（不得放到支付成功之后）
审计      🔴 不可删不可改；补偿必须填原因，且原因同步进用户用量明细页
---
计数口径  🔴 按"授权有效设备数"计（离线/从未连接均算）；90 天降级即释放配额；超限页给"最久未连接"排序
降级目标  ✅ 清授权 + 留记录（"需重新确认·已闲置 N 天"）+ 释放配额 + 提前 7 天通知 + 确认即恢复（D12-2a）
退款联动  🔴 退款=订阅终止 + 未用额度作废；已耗流量不追偿；不得用额度补偿替代退款；180 天限 1 次
SSOT      🔴 本文不得自写数值，一律引 `成本测算表` §0（铁律三）
--- ✅ D12 已拍板（2026-09-14）---
D12-1     个人版设备数 = 免费版 = **150 台**（🔴 原 100 台已由 D15 上调）⇒ 🔴 仍不作付费权益，写成"免费版优势行"
D12-2     解绑后 7 天内重绑需双验 + 90 天未连接自动降级为"需重新确认"（降级目标见 §2.4.2 `[D12-2a]`）
D12-3     延迟实名（不触发风控/不用额度/不跨账号连接前不要求）
D12-4     注销冷静期 30 天软删除（可登录撤销）
D12-5     单次补偿 > 免费额度的 10 倍需第二人复核
D12-6     P0 只自研 3 查 3 动作；退款/对账/发票/看板一律复用现成工具，不造后台
--- ✅ D15 / D16 已拍板（2026-09-14）---
D15       设备数 = 150 台（修订 D12-1 的 100）⇒ 消除「同价更少」；⚠️ 仍不作付费权益、不得当主标语
D16       日志留存 = 6 个月（到期自动删除；≥30 天冷静期；注销后匿名化）⇒ 注销级联可定稿
--- ✅ D23 / D24 已拍板（2026-09-15）---
D23       账号自助管理：P0「用户信息」= 手机号+邮箱+密码；「账号与安全」中转页必存（注销 3 步内的界面证据）
D23-1     改密后其他设备强制重登 + 恢复码作废；当前设备与已建立的远控会话不受影响
D23-2     邮箱 = 可选第二入口（不替代手机号、不得单独解锁高敏感动作、不得解绑最后一个身份入口）
D24       内部账号完全独立（邮箱+密码+强制 TOTP，不复用手机号）· 不自助注册 · 2FA 不可跳过不记住设备
          离职 = 停用而非删除（审计不可删）· 改密后所有内部台会话失效（无「当前设备」豁免）· 见 §5.7
```

---

**建立时间**：2026-09-14 · **拍板时间**：2026-09-14 · **状态**：✅ 已拍板并登记为决策 **D12**（§7 的 M1–M6 = D12-1 ~ D12-6）

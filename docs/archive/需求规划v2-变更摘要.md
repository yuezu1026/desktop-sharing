# 需求规划 v2 · v1 → v2 变更摘要（全量台账）

> 📦 由 `需求规划-v2-P0P3重构.md` 文末**原样外置**。这是逐轮变更台账（每轮一行），正文只保留条目索引。

> 📦 **来源**：`docs/需求规划-v2-P0P3重构.md` 原 L1041–1071 · 外置于 2026-09-15 · `tools/archive-split.mjs`
> 🔎 正文对应位置仍保留 `§` 编号与标题锚点，可直接在本文件内检索定位。
> 🔑 **追加新轮次**：直接追加到本文件末尾；正文 §附 处的条目索引可一并补一行。

---

## 附：v1 → v2 变更摘要

| 变更 | 说明 |
|---|---|
| 已确认 | **D1** 中继明文可见 ⇒ 禁止宣称 E2E，改「传输加密 + 直连时 E2E」+ 连接模式指示 + 中继内控；**D2** 打洞成功率目标 P0 ≥85% / 优化后 ≥92%，分桶统计 |
| 新增 | 被控端需求（§3）、非功能指标（§5）、成本模型（§8）、竞品与差异化（§9）、合规防滥用（§6.5）、Out of Scope（§7） |
| 修正 | 延迟目标分场景化；加密章节重写（去除 E2E 承诺）；认证模型重写（去掉硬件 ID 免密）；Web 端改临时码；iOS 被控标注不支持；路径 A 降为愿景；键盘遮挡与无感切换表述修正 |
| 修正（本次） | 🔴 **定位纠偏**：删「手机为核心控制端」（论证不成立），改为「PC 为被控主体 / 手机覆盖 PC 触达不到的时段」；主差异点改为「**手机作为被控端**」，新增 §3.6（移动被控需求）、§2.3（移动端交互原则）、§9.3（手机定位论证）；§5.1 指标按控制端分档 |
| 修正（本次·2） | 🔴 **移动端拆形态**：以「**身边无外接屏时手机单屏是唯一方式**」为前提 ⇒ **手持单屏 = 默认主路径（指标基线）**，**外接大屏 = P1 增强**（从 P3 提升）。§2.3 重写为两形态（含焦点跟随 / 放大镜 / 双模指针 / 纯观察模式，并**承认手持形态的可读性与命中率天花板**）；§5.1 手机档拆手持 / 外接两行；§5.2 补可读性 / 可达性 / 大屏适配率指标；§9.3 补「输出形态」正交维度；§1 P1 增外接显示器适配，P3 同步修正 |
| 修正（本次·3） | 🔴 **外接形态权重上调**：澄清它是一个**高价值项而非可选项**。新增**三种硬件组合**（纯手持 / 键鼠+手机屏 / 大屏+键鼠）与**增量硬件仅蓝牙键盘（~200 g）**的关键认识；新增手持 vs 外接的**四维对比表**；**更正上一版的「外接最后做」**（其实是 ROI 最高：成本低 × 价值高）；同时补三项风险（USB-C 单口冲突 / 发热耗电 / 现场网络）与**叙事边界**（是「应急与轻办公」不是「替代笔记本」） |
| 新增（本次·4） | 🔴 **打洞成功率的网络环境分桶（新增 §5.3）**：据 Tailscale《How NAT traversal works》（级别 B）落地四档分桶 —— 家庭↔家庭 90–95% / 一方硬 NAT ~85%（生日悖论补 1024 探测 → 98%）/ **双方硬 NAT <5%** / **封 UDP 网络 = 0%**；由此推得 4 个 P0 动作（**中继必须走 TCP·TLS 443**、**多路径并行探路**、**先中继秒连再升级直连**、主动提示切蜂窝）与 2 条工程红线（探测限速避免触发 IDS；总预算上限避免打挂客户办公室路由器）；🔴 **更正成本表方向性错误**：「酒店/家庭打洞率更高」→ 实际**酒店/企业网络才是成功率最低处** |
| 新增（本次·5） | 🔴 **被控端资源损耗（新增 §3.7）**：补上 §3.4 仅一句「占用上限待确认」的空白。把负载拆成**空闲态（硬指标，必须近零）/ 会话中（软指标）/ 与用户争抢（投诉最集中）**三类；提出**资源档位由被控端用户设定**（信任机制）；手机被控专项给出四个硬点（**省电策略杀后台 / 息屏串流采集不到真实屏幕 / 充电竞速 / 温升**）；🔴 **定位判定：PC 低损耗 = 及格线，手机低损耗 = 主差异点的必要条件**（手机被控发烫掉电 = 差异点直接崩塌）；§5.2 新增 5 行指标 |
| 修正（本次·6） `[D8]` | 🔴 **定位再纠偏：主主张回归「远程办公 / 控制电脑桌面」**。用户指出「手机应该是**控制端**」—— 该质疑成立，且暴露一处**方法错误**：D3 把「手机被控」升为主差异点时**只验了差异化、没验权重**。⇒ 新增 **D8**（§9.3）：主主张必须过**两个测试**（A 差异化 / B 权重），手机被控只过 A ⇒ 降为【**差异化能力 + 获客钩子**】（钩子 = 天然双端安装，价值在拉新不在收入）；**主主张改为商业条款「免费额度做到对手收费档位」**（主战场内无技术护城河）；§1 P0 内降为**第二优先级、不阻塞发版**；新增**对外口径红线**：禁止「免费不限量」（实际是**直连不限时长**，中继限额） |
| 新增（本次·7） | 🔴 **竞品 DeX / 桌面模式适配核实（回写 §2.3.2、§9.3、§0）**：据 TeamViewer **官方**回复（讨论 #67365）「**We support DeX on one side only** —— 从 DeX 控制别的设备可以，**反过来是 feature request**」+ RustDesk #14704（**被控端在 DeX 会话内输入完全无法注入**）+ ToDesk 移动端仅「手动横屏/全屏」⇒ **无一家做「自动桌面化布局」，外接大屏是真空白**（升为**次级差异点**，⚠️ 仍受 D6 约束不得当主主张）；⚠️ 因 **Android 16 API 37 取消大屏豁免**，窗口期约 **1–2 年**（时间窗非护城河）；🔴 **意外收获**：「**DeX 会话作为被控端**」是比外接更硬的空白 ⇒ 印证 §3.7「输入注入属硬工程」 |
| 新增（本次·8） `[D9]` | 🔴 **控制端画面显示红线（新增 §2.4）**：用户要求「电脑远程连接电脑，**不应该全屏或者让屏幕变形**」⇒ 落成**两条禁令 + 一条禁止**：① 🔴 **不得默认全屏**（默认窗口化自适应；全屏仅由用户显式触发，退出后恢复进入前尺寸与位置，断线重连不改变全屏状态）② 🔴 **不得非等比拉伸**（必须等比缩放 + 黑边填充，**比例失真 = 0** —— 非等比会让文字失真、且让用户**误判远端控件位置与真实布局** ⇒ 属**工具可信度**问题）③ **不得擅自改被控端分辨率 / DPI 去「凑」窗口**（会打乱被控端用户布局，须经其同意且会话结束恢复原状）。补**三种缩放模式**（适应窗口 / 原始 1:1 / 拉伸铺满，默认「适应窗口」）与**三个边界情况**（PC 看手机竖屏窄条 ⇒ 靠放大倍率而非拉伸 / 超宽屏黑边 / 高 DPI 不得发虚）；并厘清与 §2.3.1、§2.3.2 的边界（**画面必须真实，交互可以重做**）。§0 增「画面显示红线」行、§1 P0 增「显示渲染」行、§5.2 增 3 行指标 |
| 修正（本次·9） `[D10]` | 🔴 **PC→手机 从 P2 提升到 P0**（用户：「（电脑）…可以帮父母远程看手机的问题」）⇒ ① **增量成本≈0**（PC 控制端与 Android 被控端本就在 P0，此为其**交叉**，净增量集中在渲染与 PC 侧触控语义映射）② **否则钩子断在最有效的路径上**（D8 的钩子前提是「子女这一端连得上」，而帮父母排障时**子女更可能用电脑**：大屏看得清 + 键盘快 + 可边查边做）③ §9.3 原「PC→手机 = 少见」写在 **D8 之前**，当时语境是「手机被控 = 主差异点」，现已过时（钩子成立**不依赖控制端形态**）。⚠️ **仍属获客钩子，不升级为主主张**（D8 文案红线不变）；§0 / §1 / §3.6 / §5.2 / §9.3 / §10 同步更新 |
| 新增（本次·10） `[D11]` | 🔴 **商业化与计费设计建档并拍板（新增 `docs/商业化与计费设计.md`）** —— 回答「**怎么收钱 + 技术上怎么控**」。🔴 **三条硬结论**：① **付费墙只能挂中继**（直连不经我方服务器 ⇒ **不可估量**）⇒ **「直连不限时长」是技术事实不是策略顾让**；② **内部计量用 GB、对外展示用分钟**（按时长限额会被 2K60 用户**静默击穿**：同额度消耗、成本 **3 倍** —— 口径：2K60 = 12 Mbps vs 1080p30 = 4 Mbps，SSOT P5；旧写「8 倍」源于已作废的 16 Mbps 假设）；③ **计量裁决必须在服务端**，且**额度必须能「中途被收走」**（短周期 ticket 续期 + 30s 心跳，否则「用 1 分钟额度建立会话再挂 10 小时」）。另定 **两条红线**：**额度耗尽四级降级**（预警 → 转直连 → 降码率 → 仅查看 → 才终止）· **风控不得误伤「帮爸妈调手机」**。**D11 已拍板 5 项**：免费版中继 **2 GB/月**（⚠️ 待 TURN 报价重算）· iOS **走 IAP**（不上浮定价）· 个人版 **¥158/年** · 消耗型流量包 **P1 再上** · 额度耗尽**允许「仅查看」**。§8 免费额度 / 付费墙位置 / 配套动作三行同步更新 |
| 新增（本次·11） `[D11]` | 🔴 **计费界面（UI）清单（新增 `商业化与计费设计.md` §13）**：分为四类，其中 **①UI = 红线的执行体 / ②UI = 主主张的可见化 不适用 ROI 排序**（不做即违反已拍板决策）⇒ **P0 八项必须与免费额度同批**（原七项 + 🔴 **续费提醒**，`[B5]` 合规硬项）：连接模式指示器 · 首连「本次：直连」提示 · 免费中继余量（折叠，80% 才现）· 降级链四态（含 🔴「码率已降」明示）· 仅查看遮罩（双端）· 转直连引导页（打洞教育）· 购买页最小版 · 🔴 续费提醒（自有渠道，扣费前 5 日）。🔴 **五条界面红线**：① 技术状态与商业计数分离（否则 D1 透明度资产降为广告位）② **计费 UI 只在控制端**（被控端不得出现金额/余额/充值 —— 否则杀死「帮爸妈调手机」钩子）③ **免费的必须显式标「免费」**（否则钩子反向失效）④ iOS 无站外支付引导 ⇒ 对比页需两套 ⑤ 不做断连瞬间的促销弹窗。P1：权益对比表 · 用量明细页（反客诉主体）。⚠️ **计数已变**：`[D19]` 追加 P0 第 9 项「音频控制条」（§13.2 #9）⇒ **界面红线由 5 条增至 6 条**（新增「被控端不得被静默采音」）|
| 新增（本次·12） `[D12]` | 🔴 **账号与管理系统建档并拍板（新增 `docs/账号与管理系统设计.md`）** —— 回答「**谁来管、怎么管、拿什么管**」。🔑 **核心判断：P0 的「管理模块」不是后台**，而是极小的内部操作台 = **3 查 + 3 动作**（查用户 / 查额度与计量明细 / 查订单；调额度补偿 · 封禁解封加白 · 人工退款跳渠道后台）；**完整后台是 P1，不一开始就造后台**（造一个不如微信商户后台的退款页，收益为零）。🔴 **两条铁律**：① 涉及资金与账号权限的操作**必须走工具 + 留审计，不得直接改库** ② **后台是已拍板政策的执行体**（D11 七天可退 / §5.6 主动补偿 / **§9.3 家庭互助白名单** —— 无加白工具则该政策是空文）。🔴 **B（账号生命周期）优先级高于 A（运营后台）**。🔴 **两个最易错处**：① **「解绑」必须拆两种语义**（取消授权 vs 移除设备，混用会让旧机主仍被连）② **被控端必须能一次点击断开且不得挽留式二次确认**。⚠️ **边界：「我们自己的运营后台」≠「给企业客户的管理后台」**（v2 §7 排除的是后者）。**D12 已拍板 6 项**：个人版设备数 = 免费版 = **100 台**（不作付费权益；⚠️ **后经 `[D15]` 修订为 150 台，见本次·17**）· 解绑后 7 天重绑需双验 + **90 天**未连接自动降级 · **延迟实名** · 注销 **30 天软删除** · 单次补偿 > 免费额度 10 倍需复签 · P0 只自研极小操作台。§1 P0 增「账号生命周期最小集」行、P1 增「个人中心」与「运营操作台」两行、§6.2 补恢复码与交叉验证 |
| 新增（本次·13） `[D13]` | 🔴 **不做独立的「营销 / 增长模块」**（用户：「是否要增加一个营销模块？」）—— v2 §7 明确列为**不做项**。**最硬的理由：与 `[D12-6]`「P0 不造后台」直接冲突** —— 任何「能发东西出去」的营销模块（邀请返额度 / 优惠券 / 促销码）**必然**要求「发放 → 核销 → 对账 → 防作弊」后台。另三重理由：① **奖励正对我方唯一线性成本项** —— 竞品送授权**边际成本 = 0**，我方送中继流量**边际成本 = ¥0.30/GB 且随领取量线性增长**（`成本测算表` §1：中继流量是唯一随规模线性增长的成本）⇒ 送出去的是真金白银，且「多账号刷邀请」= **直接套现** ② **0 用户时是纯负债**（无转化基座可承接）③ **已有零成本钩子尚未用尽** —— D8/D10 的「帮爸妈调手机」= **天然双端安装**（一个用户天然带出一个新用户），且**引入现金奖励会污染其可信度**。⚠️ **同时澄清两类被误称「营销」的东西**：**投放 / ASO / 内容属运营动作（非代码模块，不进产品范围）**；**产品内推广位 / 活动中心 / 断连促销弹窗已被《商业化与计费设计》§13.4 红线 1 与 5 禁止**。⇒ 增长手段的约束写成《商业化与计费设计》**§9.4（三条红线 + 一条边界）**；`成本测算表` §10.1 新增 **#17** |
| 回写（本次·14） `[评审]` | 🔴 **业务专家全量评审回写（A1–A6 硬矛盾 + B1–B10 口径 + 完善 + 增强）**。本文（v2）改动：**§0** 主主张措辞**收窄到只允许两项**（手机被控免费 / 直连不限时长；通道数与会话数未定案前不得主张，`[B6]`）+ **北极星指标定案**（WAUS 及其分解与四条口径纪律，`[B10]`）；**§1 P0 表新增「计费与额度控制」行**（`[B1]` 曾声称已新增但实际缺失）+ **续费提醒从 P1 提升到 P0**（`[B5]`，连续包月 ⇒ 合规硬项）；**新增「P0 发版阻塞项清单」（E6）** = A 合规 7 项 / B 成本与计量 5 项 / C 体验红线 3 项 / D 外部依赖 3 项；**§5.2 从「大面积 `[待确认]`」改为「P0 验收目标」**（原状态使 P0 无法验收）+ 码率统一到 SSOT P5；**§6.3 授权码口径澄清**（`[B7]`：「Web 免登录」与「必须绑定账号」指不同主体，会话归属生成方账号）；**§6.4 结案**（剪贴板不上云 / 日志 6 个月与 D12-4 的执行顺序 / 遥测边界 `[A6]`）；**§6.5 延迟实名口径**（`[D12-3]`）；**§8 打平 MAU 参数化**（`[A1]` 作废单一数字）+ 免费额度成立前提（`[A2]`）+ 单用户成本上限修正为 ¥0.60；**§10 术语表**补 SSOT / 获客钩子 / 主主张双测试。🔴 **跨文档纪律**：数值以 `成本测算表` §0 为**唯一事实源** |
| 回写（本次·15） `[评审·二轮]` | 🔴 **业务专家第二轮全量评审落地（登记见 `需求规划评审意见.md` §9 / §10）**。本轮改动：**① 建立 SSOT 纪律** —— 数值唯一事实源 = `成本测算表` §0（本轮 6 条硬矛盾 **A1–A6** 中有 4 条源于「同一参数在多处各写一遍」）⇒ 本文、`商业化与计费设计`、`账号与管理系统设计`、`竞品对标表` 均新增「**不得自写数值**」铁律与 §0 指针；**② 口径统一** —— 码率全档回归 SSOT P5（旧 2K60=16 Mbps ⇒ 成本「8 倍」改 **3 倍**）· 免费额度以 **GB 为权威**（旧「≈60 min」改 **≙62 min**）· 打平 MAU 参数化（旧 100 万 / ~685 万 / ~700 万 **全部作废**）· 主主张措辞收窄（`[B6]`）· 续费提醒升 P0（`[B5]`）；**③ 新增交付物** —— `成本测算表` **§0 权威数值表** / **§6.3 客服产能 SLA** / **§7.5 D8 联动**、`商业化与计费设计` **§1.1 ARPPU 桥接表** / **§2.5 证据表** / **§13 计费界面** / **§14 竞争应对预案**、本文 **§1 P0 发版阻塞项清单（E6）**、本文 **§0 北极星指标 WAUS + P0 新增「计费与额度控制」行（`[B1]` 已真落）**；**④ 本轮新核实** —— 🔴 **ToDesk 专业版 = 150 台**（§9.2 硬约束 3 下方注：当时我方付费档**同价显劣已成事实**，处置 = 对比表正面披露 + 靠手机被控免费 / 直连不限时长取胜；✅ **该待拍板项已于同日拍板为 `[D15]`：两档都提到 150 台，见本次·17**） |
| 新增（本次·16） `[D14]` | 🔴 **定价必须可调，实现方式 = 商品与价格「版本化」**（用户：「产品定价不应该是固定价格，应该允许后续调价」）。上一轮笔记的保守结论（「在 `[D11-3]` 锁定 ToDesk 同档的前提下，可调价空间接近零，建议只做商品版本化 + 后台只读展示」）**已被用户否定，因为它把「要不要调」与「怎么调安全」混为一谈**：`[D11-3]` 自己就写了「上线前重新核对 ToDesk 现价（**对手会调价**）」，`[D11-1]` 又带触发器（ARPPU 跑不出 ¥18 就下调额度）⇒ **调价是刚需，问题只在实现**。⇒ 落成 **D14**（`商业化与计费设计` **§2.6**）：**① 唯一纪律：调价 = 新增一个版本，不是改那个数字**（已被订单引用的价格版本**只可新增、不可编辑** —— 订单是**财务凭证**，直接改价 ⇒ 历史订单「当时应付」无法复现 ⇒ 对账 / 退款 / 发票 / 税务 / 客诉全崩；与 §7.1 FEFO 额度账本同一原理）**② 价格与权益额度必须同版本原子快照**（防「只改价不改额度」⇒ 毛利被静默侵蚀；或「只改额度不改价」⇒ **卖一单亏一单**）**③ 四类场景必须分开支持**（新价对新人 / 新价含存量续费 / 限时活动价（**到期自动回落 + 提前提醒**）/ 渠道分价）**④ iOS 价源 = Apple**（App Store Connect 是唯一价源 ⇒ 我方后台**只读镜像、不得编辑**，否则必然出现「后台 ¥18 / Apple 实扣 ¥24」口径分裂）**⑤ 五条红线**：不改已被订单引用的版本 · 不静默改存量续费价 · 不拆散「价格+权益」 · 不设无依据划线价 · 后台价 / 展示价 / 渠道实扣价 / 续费实扣价必须同源。**落地时点 = P1 完整运营后台**（与 `[D12-6]`「P0 不造后台」**不冲突**，且正落在「渠道商户后台做不到、必须自研」那一类）⇒ 本文 §1 P1 增「商品与定价版本管理」一行；`账号与管理系统设计` §5.3 / §5.4 / §6 同步（🔴 **定价变更 = 管理员 + 双人复核 + 必填原因 + 预约生效**，**不得授予客服**：能改价 ⇒ 能自造折扣 ⇒ 内部套利）✅ **四个子决策已于同日全部拍板**（用户采纳建议默认）：**`[D14-1]` 涨价不对存量迁移（按旧价续，`renew_price_policy` 默认 `PIN_TO_SIGNED_VERSION`）** · `[D14-2]` 降价自动惠及存量 + 必须显著告知 · `[D14-3]` 活动价自动回落原价 + 提前 5 日提醒 · `[D14-4]` iOS 价源 = Apple（后台只读镜像）|
| 新增（本次·17） `[D15][D16][D17]` | 🔴 **三项待拍板按建议默认值拍板（`需求规划评审意见` §10.5 已清空）**。**`[D15]` 设备数 = 两档都提到 150 台（B 案）**：原 `[D12-1]` 的 100 台 ⇒ ToDesk **专业版 ¥158/年 = 150 台**（**同价**）⇒ 原「同价位显劣」会被截图打脸；且设备数**边际成本 ≈ 0**（不连就不花中继）⇒ 直接给足。SSOT = `成本测算表` §0 **P15**；路线对比见 `竞品对标表` §2.5a。🔴 **口径三条**：① **仍不作付费权益**（两档相同 ⇒ 权益对比表写成「**免费版优势行**」）② ✅ **可并列陈述事实**（「对手付费档位的设备数，我们免费版就给」）③ ❌ **不得当主标语**（用户不会为设备数付费）。**`[D16]` 日志留存 = 6 个月**（到期自动删除；🔴 不得短于 `[D12-4]` 的 30 天冷静期；注销后必须可匿名化；删除必须是自动任务）⇒ **解除了 `[D12]` 的「注销流程前置依赖」**（`成本测算表` §10.1 **#16 已结案**）。**`[D17]` 反诈 / 实名 = 可验收的默认路径**：`[D12-3]` 延迟实名已定 ⇒ 本文 **§6.5 补三类触发事件表**（触发风控 / 使用中继额度 / 跨账号连接）+ 未实名降级路径 + 被控端二次确认文案位 + 异常连接拦截；**P0 验收 = 触发链路与入口可用**（非「人人先实名」）；⚠️ **保留「最终文案须法务复核」标记**（法务 3 问：实名数据留存边界 / 内容安全处置义务边界 / 资质判定）⇒ **不阻塞开发，属 P0 发版阻塞项**；**§1 E6 清单合规组新增 A8（反诈风险提示位，不得折叠隐藏）⇒ A 组由 7 项变 8 项**。受影响回写：本文 §0 / §1 P0 / §1 E6 / §6.4 / §6.5 / §8 / §9.2；`成本测算表` §0 P15 / 日志成本行 / C3 / §10.1 #16；`商业化与计费设计` §1 / §2.5 / §2.5a / §13.3；`竞品对标表` §2.5 / §2.5a / §9 #20；`账号与管理系统设计` §2.3 / §2.3.1 / §2.6 / §2.7 / §6 / §7 / §8 |
| 新增（本次·18） `[D18]` | 🔴 **成本参数（TURN 报价）必须后台可配、后续可调**（用户：「这个报价不能通过管理后台管理起来吗？后续可以调整？」）。🔑 **这是 `[D14]` 的镜像补全，不是新理念**：`[D14]` 让**售价**可调，`[D18]` 让**成本**可调 —— **缺了后者，前者就是盲调**（只知售价、不知成本）。**先分清两类「报价」**：**(A) 输入成本**（TURN 单价 $P$ / 方向系数 $k$ / 保底带宽）= **采购合同参数，我方只能录入收到的报价**；**(B) 由 (A) 派生的产品参数**（免费额度 / 码率上限 / 降级阈值 / 对外「分钟」折算）= **必须后台可配、可版本化、可热调**。**落地 = 两层模型**：`cost_param`（人填 + 版本化 + 必填报价来源）+ `derived_metric`（🔴 **系统算，禁止手填**）；时点 = **P0 定模型与入口、P1 出界面**（与 `[D12-6]`「P0 不造后台」不冲突）。**四个子决策已按建议默认值拍板**：`[D18-2]` 单价**必须带档位语义**（四档择一 + 可覆盖，❌ 不做裸数字框）· `[D18-3]` $k$ **强制显式、无默认值**（未填 ⇒ 系统拒绝对外额度输出）· `[D18-4]` 权限 **仅负责人可改 · 财务只读 · 客服完全不可见** · `[D18-5]` **必填报价来源**。🔴 **`[D18-6]` 口径修正：TURN 报价从「元阻塞」降级为「待填参数」**（**阻塞对外口径，不阻塞开发**）—— 二者正交：**阻塞的本质 = 不知道该填什么**（只能问厂商），`[D18]` 解决的是**改起来多快**。⚠️ **$k$ 不是可调参数，是商务条款**；$k$=2 ⇒ 成本翻倍 ⇒ `[D11-1]`（2 GB）**除以 2.7** ⇒ **免费版主主张被击穿**。⇒ 本文 **§1 B1 标签已改**；`成本测算表` **§3.4 新增** + §0 P1/P2 / 使用规则 / §10.2 / §11；`商业化与计费设计` **§2.7 新增**；`账号与管理系统设计` **§5.3 / §5.4** |
| 重构 | 手机独立远控独立成章（并入 §2 核心体验）；消除重复章节；全篇按 P0–P3 组织 |
| 新增（本次·19） `[D19]` | 🔴 **远程音频进 P0 及格线 + 影音 / 游戏能力划边界**（用户：「是否支持远程看视频 / 打游戏 / 听音乐？微信在远程能否远程接听 / 语音 / 视频？」+「刚才说的功能很重要，也是亮点」）。🔑 **先判断是否刚需、再谈怎么安全地做**：音频不是「增值功能」，是 **D8 钩子的另一半** —— 「帮爸妈看手机」只解决**看到**、不解决**说清**（排障一半时间花在「你点那个」「哪个？」的来回转述上）⇒ 缺席即**钩子只完成一半**。**落成三合一音频通道**（**D19-1**）：① **系统音频回传**（Win WASAPI loopback / macOS 13+ ScreenCaptureKit / Android `AudioPlaybackCapture`）② **语音对讲**（控制端麦克风 → 被控端扬声器，**播放不需驱动**）③ **纯音频模式**（关画面只传声）。**D19-2** 由此扩出「**远程陪伴 / 远程照料**」战场（与「帮爸妈」同源），⚠️ **但不改变 D8 主主张与品类定义**。**D19-3 微信口径**：✅ 支持「**边看边说**」（走自建语音通道）；⚠️ **虚拟麦克风 / 摄像头注入降为 P1**（需签名内核驱动）；❌ **不承诺** 安卓通话音频（`AudioPlaybackCapture` 只允许 `MEDIA`/`GAME`/`UNKNOWN`，通话被系统排除）/ DRM 内容 / iOS 被控 / 竞技网游。**D19-4 影音游戏**：✅ **P1 新增「高帧率影音档」**（2K60 / 4K60 + 音频增强，主打影音 + 单机 / 策略 / 卡牌）；🔴 **反作弊风险必须在购买页明示**。**D19-5 计费**：✅ 音频**计入会话总量**（实占中继，仅视频的 3% ⇒ 不单开科目）；🔴 **纯音频模式必须单列更低口径**（否则「只听声音」与「看 1080p30」扣同样多，成本差 40 倍）。**D19-6 验收**：音画同步（滞后 ≤ 125 ms / 超前 ≤ 45 ms `[需核实]`）· 音频断连不中断画面 · 被控端静音即时生效。🔴 **隐私红线**：**被控端不得被静默采音** —— 必须可见提示 + 一键静音（《商业化与计费设计》§13.4 红线 6）。受影响回写：本文 §0 / §1 P0 / §1 P1 / §4.4 / §5.2 / §7；`成本测算表` §0 **P16 P17** / §3.2.1 / §3.3 / §10.1 #20 #21 / §11；`商业化与计费设计` §1 L4b / §1.1 / §2.2 / **§5.7** / §13.2 #9 / §13.4 红线 6 / §12；`竞品对标表` §6 矩阵；`需求规划评审意见` §7 **D19** / §10；线框 `README` / W1-10 / W2-08 |
| 新增（本次·20） `[D20][D21]` | 🔴 **第二轮 UI/UX 评审的两项待拍板（Q1 计量口径 / Q2 实名与白名单边界）已按建议方案拍板**。**`[D20]` 对外计量展示口径**（本文 §1 A6 关联 · `商业化与计费设计` **§5.1.1 新增**）：分钟为折算展示值，四条硬规则 —— ① **必须带注脚「按当前画质估算 · 切换画质会变」**，GB **只在用量明细页**出现并注明「GB 是权威计量」② 🔴 **折算取码率地板 `max(实际码率, 2 Mbps)`**（SSOT **新增 P18**）⇒ 折算值**只随画质升高而变小，永不因降级而虚增** ③ 🔴 **纯音频模式（SSOT P17 = 0.1 Mbps）不参与分钟折算**，改显示「剩余 xx GB / 按纯音频口径计」（否则 2 GB 会显示成 **≈2480 分钟**，与 4K60 的 ≈12 分钟差 **200 倍**，一张截图即成定价事故）④ **扣减仍按 GB、不得改按分钟扣** ⑤ **画质切换须主动提示**（与「禁止静默降码率」同源、方向相反：升码率的代价也不得静默）。**`[D21]` 实名闸门迁移 + 白名单落点**（本文 **§6.5.1 新增**）：① ③「跨账号连接 ⇒ 控制端实名」**改主体**为「**被控端二次确认 + 展示控制端可识别信息**」② ②「中继实名」由未定义的「超过免费额度阈值」改为 🔴 **阈值（免费额度 50% ≈ 1 GB/月）或 频次（7 天内 ≥ 3 台不同被控设备）双条件** ⇒ 「帮爸妈调手机」（低频 + 1~2 台）**两条都不命中**，薅额度者**必命中** ③ 🔴 **白名单必须有用户侧自助入口**（原唯一落点是内部运营台 ⇒ 现实中要「找客服加白」）：被控端「**受信任的控制端**」列表（W2-06）+ 控制端「**家人设备**」标记，两侧都标 ⇒ 记为**可信配对** ⇒ 不触发 ④；⚠️ **P0 只做声明不做关系审核**，🔴 **不得新造绑定系统**（违 `[D12-6]`）④ 🔴 **豁免 ≠ 弱化提示**：**A8** 反诈提示**不得折叠、不得降权**，且建立「受信任的控制端」这一动作**必须再确认一次反诈** —— 🔴 反诈场景与钩子场景**特征同构**（低频 + 异地 + 新设备 + 老人）⇒ **只能靠提示强度区分，不能靠身份门槛区分**；**放宽的是身份门槛，收紧的是知情同意**。⚠️ 内部运营台「加白」动作保留但**降为「例外处理」**（兜底而非主路径）。受影响回写：本文 §1 **A6** / §6.5 / 本变更表；`成本测算表` §0 **P7 P17 + 新增 P18** / §7.2；`商业化与计费设计` **§5.1.1** / **§9.3.1** / §13.2 #2 #9 / §13.3 / §13.5 / §12；`账号与管理系统设计` §5.1；线框 **W1-04 / W4-05 / W5-03 / W5-06 / W2-02 / W2-06** + `README` §11.5.3 |
| 新增（本次·21） `[D22]` | 🔴 **全屏 / 沉浸式会话（本文 §2.4.1 新增）** —— 回答用户「浏览器、PC、手机远程连接 PC，**全屏时能否实现沉浸式体验**」。🔴 **诊断**：原 §2.4 只有「**不得默认全屏**」一条**禁令**，**没有定义全屏态本身**（长什么样 / 怎么退出 / 指示器还在不在），而全屏正是**唯一会把所有界面元素让给画面**的状态 ⇒ 也是**最可能把 D1 连接模式指示器挤掉**的状态。**落成三条**：① 🔴 **红线 3：全屏期间连接模式指示器不得消失**，只允许**降级为极简角标**（`● 直连` / `● 中继` 二字 + 一个连接质量点）—— 沉浸感靠**减法**（工具条自动隐藏 / 边框归零）实现，**不靠删掉指示器**（它是 D1 在全屏下**唯一还能承载主主张的元素**，`商业化与计费设计` §13.2 #1 明写「常驻」）② **四条实现要求**：`[D22-1]` 全屏仅用户显式触发（快捷键**须在设置页可见可改**）· `[D22-2]` **工具条静止 3 s 淡出 + 顶边悬停 / 快捷键两种唤出**（🔴 **不得抢占被控端鼠标**）· `[D22-3]` 退出恢复进入前尺寸与位置、断线重连不改变全屏状态 · `[D22-4]` **多显示器全屏拼接**且切换须可在全屏内完成 ③ 🔴 **浏览器端天花板（必须写进 Web 控制端那单独一稿）**：Fullscreen API **只能用户手势触发**（与 D9 方向一致 ✅）/ **ESC 强制保留、页面不得拦截**/ **F11 页面拿不到按键**、也无法隐藏浏览器 chrome ⇒ 网页端全屏**必然仍有一层浏览器** ⇒ 🔴 **不得承诺「与客户端一致的沉浸式体验」**，对外只能说「网页版可以全屏」（否则用户第一次按 ESC 就穿帮） ④ 🔴 **手机作控制端**（沉浸式收益最大、也最容易做错）：系统状态栏 / 导航栏本身吃屏 ⇒ 收益远大于 PC，但**必须显式定义三件事**：唤出只能靠**边缘手势**（不得依赖顶部下拉，那是系统栏已占用的手势）· **虚拟键盘与指针模式必须可切换**（否则全屏后无法输入＝远控变只读）· 退出**两条路径**（工具栏 + 系统返回手势）；🔴 **仍不得默认全屏**（用户很可能边看手机边操作 PC，自动全屏会让他切不了 App）。受影响回写：本文 §0 画面显示红线行 / §1「显示渲染」行 / §2.4 红线 1 表 / §5.2 **C1**；`商业化与计费设计` §13.2 #1 / §13.5 / §12；线框 **新增 W1-11 + W6-09** + `README` §2 / §7 / `index.html` |
| 新增（本次·22） `[D24]` | 🔴 **运营台认证与内部账号（`账号与管理系统设计` §5.7 新增）** —— 回答用户「**是否存在系统管理后台的登录，忘记密码，修改密码等的 UI 设计？**」。🔴 **诊断**：全稿的运营台三帧（W5-04 ~ W5-06）**全部从「已登录 + 已带入角色」开始**，而该台能查**全网用户的手机号 / 实名状态 / 每笔用量**，还能**改额度、封禁、退款** ⇒ 它是**全系统风险最高的入口**，却是全稿**唯一没有「门」的界面**；⇒ `账号与管理系统设计` §5.4 的 P0 硬项「**权限必须从第一天就分开**」**没有身份来源**、§5.5 审计的「**谁**」**没有主语**。⚠️ **与 `[D12-6]` 的关系必须说清**：「P0 不造后台」= 不做**完整后台**（定价版本 / 成本台账 / 报表 / 工单），🔴 **不含访问控制**；且 §5.1 的 3 查 3 动作与 §5.4 / §5.5 **本就已经是 P0**。**落成 `[D24]`**：① 🔴 **完全独立账号 = 邮箱 + 密码 + 强制 TOTP**，**不复用手机号 / 短信 / 第三方登录** ② 🔴 **不提供自助注册**（开立 / 停用只由管理员，均进审计） ③ 🔴 **2FA 不可跳过、不设「记住设备」** ④ 🔴 **离职 = 停用而非删除**（审计不可删） ⑤ 🔴 **改密后所有内部台会话立即失效**（**无** C 端 `[D23-1]` 的「当前设备保持登录」豁免 —— 内部台凭据泄漏 = 全网数据泄漏） ⑥ 🔴 失败提示**只报**「账号或密码错误」（不得成为内部账号枚举接口）+ 连续失败锁定告警。受影响回写：本文 **§1 P1 运营操作台行**（就地订正「P1 完整版」标注）/ **§6.2**（内部账号独立体系，**不并入 C 端三层模型**）/ 本变更表；`账号与管理系统设计` **§5.7 新增** + §5.4 / §5.5 / §6 / §7 / §9 / 附录速查；线框 **新增 W5-07 + W5-08** + `README` §1 / §2 / §2.6 / §3 / §4 / §5 / §6 / §7 / §11.8 + `index.html`。⚠️ **仍未出稿（新登记）**：管理员侧「开号 / 角色分配 / 停用」界面（P0 由管理员按内部流程执行） |
| 新增（本次·23） `[D25]` | 🔴 **在线支付链路（`商业化与计费设计` §13.8 新增）** —— 回答用户「**是否有用户在线支付的UI设计？**」。🔴 **诊断**：全稿**只有「购买页」（下单前），没有「支付本身」** —— 而支付是全流程**唯一「钱已经出去、权益还没到」**的状态；缺它 ⇒ 用户的唯一求助路径是**找客服**。逐环节清点：商品 / 价格 ✅ · 支付方式 ⚠️**形同占位**（W4-01 只有两枚占位按钮）· 订单确认 ❌ · 支付结果 ❌ · 已付钱未到账 ❌ · 自动续费扣款失败 ❌（W4-08 只画了扣费前提醒 = **只画了一半**）· 账单 / 发票 ✅。⚠️ **边界**：微信 / 支付宝的**收银台页**与 Apple 的**支付弹层不是我方界面，不出稿**；需出稿的只有**跳转前 / 跳转中 / 跳转后**三段。⚠️ **一处清单盲点**：商业化 §13.1 四行分类全是「**状态告知型**」，**无「交易型界面」归属**。**落成 `[D25]`**：① 🔴 **支付结果必须画全四态**（已开通 / 未完成 / **确认中** / 订单已关闭）—— **「确认中」是「钱出去、权益没到」的唯一出口** ② 🔴 **不得猜测失败原因**（渠道不回传 ⇒ 猜错等于指控用户）③ 🔴 **不得自动重试扣款** ④ 🔴 **失败 ≠ 终止**（重试 → 7 天宽限，**不得静默降级**）⑤ 🔴 **订单必须固化下单当时的价格版本**（`[D14]` 的用户侧唯一可见位）⑥ 🔴 **支付结果页与订单确认页不得有任何推荐位**（同 §13.4 红线 5）⑦ 🔴 **Web 购买页与 iOS 版（W4-02）是两套实现，不得合并**（红线 4）⑧ 🔴 **发票抬头必须在支付前可填**（兑现 W4-01 承诺）。受影响回写：本文 §1 P0「计费与额度控制」行 / **§6.2** / 本变更表；`商业化与计费设计` **§13.8 新增** + §13.1 / §13.2 **#10~#13** / §13.5 / §12；`账号与管理系统设计` **§4.6 新增** + §4.3 / §4.3.1 / §4.4 / §5.5 / §6 / §7 / §9.1 / 附录速查；线框 **新增 W4-09 + W4-10a~d + W4-11 + W4-12（共 7 帧）** + `README` §1 / §2 / §2.4 / §4（新增流 7）/ §7 / §11.9 + `index.html`。⚠️ **仍未出稿（新登记）**：独立可管理的**发票抬头 / 税号列表页** · **支付渠道管理页**（绑卡在渠道侧）· 中继流量包购买（`[D11-4]` **P1 再上**）|

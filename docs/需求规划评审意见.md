# 《个人消费级远程桌面需求规划》评审意见

- 评审对象：`docs/个人消费级远程桌面需求规划.md`
- 评审日期：2026-09-14
- 评审结论：**方向有亮点，但不具备立项条件** —— 当前文本属于「功能愿望清单」，尚不是「可交付、可排期的需求规划」。

---

## 0. 总体判断

| 维度 | 评价 |
|---|---|
| 方向与差异化 | ✅ 好。智能眼镜接入 + 鸿蒙深度优化是真差异点 |
| 完整性 | ❌ 被控端（Host）需求、成本模型、合规与防滥用 三块整块空白 |
| 一致性 | ❌ 存在 4 处硬性自相矛盾 |
| 可验证性 | ❌ 除延迟一个数字外，无任何量化指标，无法验收 |
| 可排期性 | ❌ 无优先级、无范围边界（Out of Scope）、无里程碑 |

**核心矛盾一句话**：定位是「个人消费级」，功能清单却是「企业级堆砌」；消费级真正的生死线（被控端体验、带宽成本、防滥用合规）反而全部缺失。

---

## 1. 硬伤：自相矛盾 / 逻辑不成立

### 1.1 【严重】性能目标与接入路径物理上互相打架

> 原文：`实现超低延迟（如低于15ms甚至3ms）`

| 场景 | 现实 RTT 量级 | 与 3ms 的差距 |
|---|---|---|
| 同局域网 | 1–5 ms | 可达 |
| 同城公网 | 20–40 ms | 差一个数量级 |
| 跨省 / 跨运营商 | 50–100 ms | 差两个数量级 |
| 蓝牙桥接眼镜 | 30–100 ms（蓝牙协议本身） | 差两个数量级 |
| Web 端（浏览器 JitterBuffer 保守） | 天然高于原生客户端 | 直接冲突 |

同一份文档里既承诺 3ms，又主推 Web 端直连、蓝牙桥接、Miracast 无线投屏 —— 后者**物理上不可能**达到前者。

**修改建议**
- 改为**分场景 SLA**：局域网 <10ms，同城 <40ms，跨地域 <100ms，蓝牙桥接 <150ms。
- 统一口径为「**端到端 P95 延迟**」（采集 → 编码 → 传输 → 解码 → 渲染），不是网络 RTT。二者常被混用，是最常见的验收扯皮点。
- 补充配套指标：帧率、分辨率档位、带宽占用、首帧时间（TTFF）、重连耗时、丢包恢复策略。

### 1.2 【严重】「端到端加密」的对外承诺与实际架构不符 🔴 **已确认**

> **决策（2026-09-14）**：中继节点**明文可见**。技术上做不到对服务器隐藏内容，因此**本产品不得对外宣称「端到端加密」**。

这是本次评审最需要**立刻改口**的一条，理由有三：

1. **法律风险**：架构上服务端可解密，宣传却称「端到端加密」⇒ 与实际情况不符且足以影响购买决策，构成**虚假广告**（《广告法》第二十八条）。
2. **技术表述错误**：原文 `采用 AES-256 等高强度加密算法保护链路安全` 混用了「链路加密」与「端到端加密」两个层级。AES-256 只是算法外壳，**密钥由谁生成、谁持有**才决定加密层级。
3. **表述内部冲突**：中继明文可见与「全链路操作日志」是自洽的；真正矛盾的是把二者与「端到端加密」写在**同一段**。

**正确的表述分层**

| 连接模式 | 加密性质 | 对外可宣称 |
|---|---|---|
| P2P 直连（打洞成功） | 真端到端加密，服务器不经手内容 | ✅ 可称「端到端加密」 |
| 中继转发（打洞失败） | **传输加密**（TLS 1.3 / DTLS），服务器可解密 | ✅ 「传输加密」　❌ 不可称 E2E |

**修改建议**
- 对外统一文案：**「全链路传输加密；P2P 直连时端到端加密」** —— 诚实，且不丢失卖点。
- 客户端**显示当前连接模式**（直连 / 中继）让用户知情，这也是竞品普遍缺失的体验点。
- 技术实现：握手 X25519/ECDH + 前向保密（PFS），会话 AEAD（AES-GCM 或 ChaCha20-Poly1305），密钥由 KDF 派生。
- **中继明文可见 ⇒ 必须新增内控要求**（强制项）：
  - 明文**不落盘**，仅内存转发；
  - 运维访问需**审批 + 全程审计**，防止内部人员偷看；
  - 明确日志留存期限与到期删除机制。
- 隐私政策**明示告知**：「为保障连接可用性，P2P 打洞失败时，您的数据将经服务器转发。」

### 1.3 【严重，且是安全漏洞】指纹章节信任链自相矛盾

> 原文 5.1：`在被控端手机上安装客户端后，用户只需通过手机指纹验证，即可自动生成一次性加密令牌`
> 原文 5.2：`绑定设备指纹（如硬件ID、TPM芯片标识等），实现客户端级别的自动信任…彻底告别密码输入流程`

| 问题 | 说明 |
|---|---|
| 主体写错 | 5.1 说的是「被控端手机」，但被控端主体应是电脑 |
| **把「设备识别」当成「身份认证」** | 硬件 ID / TPM 只能证明「这是同一台机器」，**不能证明「操作者是你」** |
| 后果 | 仅凭硬件绑定即免密登录 ⇒ **设备被偷 / 被转卖 = 账号直接失守**。这是漏洞，不能当卖点写 |

**正确的三层模型**

```mermaid
flowchart LR
    A["账号（身份根）<br/>密码 / 短信 / 恢复码"] --> B["设备授权<br/>首次连接需人工确认一次"]
    B --> C["生物识别<br/>仅用于本地解锁私钥<br/>换取短期 Token"]
```

- 生物特征**永不离开设备**（TEE / Secure Enclave）。
- Token 应短期（分钟级）+ 可吊销，而非长期免密。
- 5.4 的方向是对的，**5.2 必须重写**。

### 1.4 【严重】路径 A / 路径 B 划分混乱，且路径 A 当前不可行

文档自述「当前智能眼镜算力与功能有限」，却把路径 A（眼镜直连、AI 眼镜直连 PC、语音操控 PPT）与路径 B 并列为「双路径都要做」—— **不可行路径不能与可交付路径并列进排期**。

**根本问题是「智能眼镜」未定义品类：**

| 品类 | 代表产品 | 本质 | 能否跑客户端 |
|---|---|---|---|
| 显示型（BB 光学） | Xreal / Rokid | 外接显示器 | ❌ 不能 |
| AI 型 | Ray-Ban Meta | 无显示能力 | ❌ 不能 |
| 一体机 | 暂无成熟消费级产品 | 独立 Android | ⚠️ 理论可行 |

- **路径 B（眼镜 = 外接显示 + 音频）对显示型眼镜成立** → 当前可交付。
- **路径 A 需要「一体机 + 开放系统 + 允许跑 SDK」** → 目前基本不存在，属长期愿景。
- 另：「USB Type-C 直连并切换电脑模式」**不是软件能单方面实现的**，依赖眼镜支持 **DP Alt Mode** + 手机支持桌面模式（三星 DeX / 华为 PC 模式）。必须标注硬件前提。

**修改建议**：路径 B = 可交付路径；路径 A = 长期愿景，标注触发条件，**不做工程承诺**。

---

## 2. 重大缺口（整块缺失的需求）

### 2.1 🔴 被控端（Host）需求几乎空白 —— 最大缺口

全文约 90% 是**控制端视角**。而消费级远控的投诉 TOP1 全部发生在被控端：

| 缺口 | 说明 |
|---|---|
| **无人值守** | 「公司电脑 → 家里电脑」「帮父母修电脑」是核心场景。被控端是否需要点「同意」？如何授权？ |
| **睡眠 / 唤醒** | 消费级电脑默认休眠 ⇒ 直接连不上。是否支持 Wake-on-LAN？软唤醒方案？ |
| **UAC 提权** | 远程时 Windows UAC 安全桌面弹窗无法被普通用户态进程点击。这是全行业经典难题，必须专门设计（安装为系统服务 + 提权代理进程） |
| **开机自启** | 服务态（开机即可连）与用户态（登录后可连）是两条独立链路，都要覆盖 |
| **多显示器** | 切换 / 扩展 / 镜像、分辨率与 DPI 自适应 |
| **硬件编码** | NVENC / Quick Sync / VideoToolbox / AMF。不做硬编，笔记本 CPU 直接被吃满，消费级不可接受 |
| **被控端资源占用** | 空闲内存占用、CPU 占用上限、静默升级 |
| **多用户 / 会话切换** | Windows 快速用户切换、锁屏态连接 |

> 这块不是「补充」，而是**产品的另外一半**。

### 2.2 🔴 成本模型缺失 —— 消费级的生死线

- **P2P 打洞成功率是多少？失败后走中继（TURN）的带宽费谁掏？** 中继带宽是这类产品的**第一大成本项**。
- 没有成本约束，就不敢承诺「免费 / 不限时」—— 商业上不成立。
- 缺失内容：免费额度、付费墙位置（并发数 / 时长 / 画质 / 商用授权）、单用户成本上限、打洞成功率目标。

**🔴 已确认（2026-09-14）：必须设定打洞成功率目标**。建议值如下，最终数值需实测后锁定：

| 阶段 | 目标 | 说明 |
|---|---|---|
| P0 上线 | **≥ 85%** | 移动网络 + 企业 NAT 环境下的实际值，属行业常见区间 |
| 优化后 | **≥ 92%** | 增加端口预测、多 STUN、UPnP / NAT-PMP、IPv6 通道等手段 |
| 监控要求 | 按 **NAT 类型 / 运营商 / 网络环境分桶统计** | 否则无法定位失败原因，也无法针对性优化 |

> **成本推算逻辑**：中继带宽消耗 ≈ `(1 − 打洞成功率) × 总会话时长`。
> 以 85% 为基准，成功率每提升 1 个百分点，中继成本同比下降约 **6.7%** —— 打洞成功率是**唯一能直接压缩第一大成本的杠杆**。

### 2.3 🔴 合规与资质（中国市场尤致命）

- 远控软件在国内需**强制实名认证**（ToDesk / 向日葵均如此），部分场景涉及**增值电信业务许可**。
- **防滥用机制是硬性要求**：被控端显式二次确认、反诈风险提示、异常连接拦截、异常行为风控。否则产品会被用于电信诈骗 ⇒ **应用商店下架 + 商誉毁灭**。当前文档零提及。
- 个保法：剪贴板内容是否上云？日志留存期限？需明示同意与最小化采集。
- **iOS 能力需标注清楚**：App Store 对远控类审核极严，iOS 通常**只能作为控制端**，作为被控端（控制手机屏幕）基本不可能。文档写「iPhone / Android 全平台互控」是**过度承诺**，应直接标注「iOS 作为被控端：不支持」。

### 2.4 🟠 差异化未论证

文档列出的能力 —— 账号体系、剪贴板、文件传输、隐私屏、权限管理、指纹登录 —— **竞品（向日葵 / ToDesk / AnyDesk / RustDesk / TeamViewer）全部已有**。真正独有的只有智能眼镜接入 + 鸿蒙深度优化。

**必须回答：用户凭什么从现有产品迁移过来？** 建议补一份对标表（功能 / 价格 / 延迟 / 差异点）。

### 2.5 🟠 认证与账号找回的兜底缺失

「指纹免密」与「忘记密码怎么办」天然对立。缺：账号找回路径（邮箱 / 短信 / 恢复码）、设备解绑、解绑上限与冷却期、换机流程、账号可绑定设备数量上限、账号被锁后的应急通道。

> ✅ **已完成（2026-09-14）**：见 **`账号与管理系统设计.md`** §2（含本节漏写的**账号注销**）。
> 🔴 该文补了三个 §2.5 未预见的点：① **「解绑」必须拆成两种语义**（取消授权 vs 移除设备，混用会造成隐私事故）② **换机是隐私事故高发场景** ⇒ 需「一键解绑全部」+ 90 天授权复核 ③ **账号注销 ≠ 取消订阅**。

#### 2.5a ✅ 已拍板（2026-09-14，登记为 **D12**）

| 项 | 已拍板结果 |
|---|---|
| **D12-1** 个人版设备数上限 | **与免费版一致 = 150 台**（✅ **原 100 台已由 `[D15]` 上调**，2026-09-14）⇒ 🔴 **仍不作付费权益**，在权益对比表里写成「免费版优势行」；✅ 可并列陈述事实（= 对手 ¥158/年 专业档位），❌ 但不得当主标语 |
| **D12-2** 解绑冷却期 + 授权复核 | **解绑后 7 天内重绑需双重验证** + **90 天未连接自动降级为「需重新确认」** |
| **D12-3** 实名时机 | **延迟实名**（不触发风控 / 不用中继额度 / 不跨账号连接前不要求） |
| **D12-4** 账号注销冷静期 | **30 天软删除**（可登录撤销） |
| **D12-5** 大额复核阈值 | **单次补偿 > 免费额度 10 倍需第二人复核** |
| **D12-6** P0 运营工具形态 | **自研极小操作台（3 查 + 3 动作）+ 退款 / 看板复用现成工具**；❗**P0 不造后台** |

> 🔴 **D12 的三个核心判断**：① **P0 的「管理模块」不是后台**，是 **3 查（用户 / 额度与计量明细 / 订单）+ 3 动作（调额度补偿 · 封禁解封加白 · 人工退款跳渠道后台）** ② **后台是已拍板政策的执行体** —— 无加白工具则 §9.3「家庭互助」是空文，无补偿工具则 §5.6 是空文 ③ 涉及资金与权限的操作**必须走工具 + 留审计，不得直接改库**。
> ⚠️ **边界**：本节说的是**我们自己的运营后台**，**不等于**「给企业客户的管理后台」（后者已由 v2 §7 明确不做）。

### 2.6 🟠 Web 端约束未写清

- 浏览器无法安装驱动 / 系统服务 ⇒ **Web 只能做控制端，不能做被控端**（除非用户手动授权屏幕捕获，且体验与能力受限）。
- 「临时借用电脑」场景下**强制登录账号本身有盗用风险**（共用电脑、密码被浏览器记住）。应支持**一次性授权码 / 临时码**模式，而非登录账号。
- 需写明：HTTPS 强制、iOS Safari 限制、屏幕捕获授权弹窗、剪贴板 API 权限限制。

### 2.7 🟡 其他缺失清单

- 文件传输（仅在「动态权限」里被顺带提过一次，无独立需求）
- ~~**实时语音通话** —— 帮父母 / 同事排障时「边看边说」是刚需，竞品均有~~ ⇒ ✅ **已解决（本次·19 `[D19]`）**：已升级为 P0，且**不止语音对讲** —— 落成**三合一音频通道**（**系统音频回传** + **语音对讲** + **纯音频模式**）。🔴 **定性修正**：它不是「缺失的可选功能」，是 **D8 钩子的另一半**（只看不听 ⇒ 只能靠对方转述「你点一下那个…」）⇒ 见 v2 **§1 P0 / §4.4**、`竞品对标表` **§6 / §6.6**
- 远程打印
- 断网 / 弱网恢复策略、弱网自动降级（降分辨率 / 降帧率 / 降画质）
- 跨 NAT 类型兼容清单（对称型 NAT 处理）
- 安装包体积、内存占用、启动时间等消费级敏感指标
- 自动更新机制（灰度 / 回滚）

---

## 3. 结构与表述问题

| # | 位置 | 问题 |
|---|---|---|
| 1 | 第三部分 | **层级错乱**：标题是「智能眼镜与手机融合」，却把「手机独立远程连接（核心能力）」并列同级。手机独立使用与眼镜无关，应独立成章 |
| 2 | 一.1 vs 五.1 | **重复**：统一账号与指纹一键登录内容重叠 |
| 3 | 一.3 vs 二.1 | **重复**：Web 端 与 跨平台覆盖重叠 |
| 4 | 二.2 | **逻辑跳跃**：`双指缩放…解决手机远控时键盘遮挡输入框的痛点` —— 键盘遮挡输入框是本地输入法 / UI 问题，与远程画面手势无关。手机远控真正的键盘问题是「远程输入法与本地输入法不一致」以及「软键盘遮挡远程画面」 |
| 5 | 三.路径B.2 | **过度承诺**：`摘下眼镜…体验平滑过渡无感切换` —— 音频路由从眼镜切回手机扬声器存在 200–500ms 中断。应改为「自动切回，耗时 <1s」 |
| 6 | 全篇 | **缺章节**：竞品分析、用户画像与场景量化、北极星指标、**Out of Scope（不做什么）**、P0–P3 优先级、里程碑 |

---

## 4. 术语统一建议

| 术语 | 当前用法 | 建议统一为 |
|---|---|---|
| 延迟 | 「低于15ms甚至3ms」 | **端到端 P95 延迟**（采集→编码→传输→解码→渲染），并分场景给出 |
| 加密 | 「端到端加密」「AES-256 保护链路安全」 | **已确认中继明文可见 ⇒ 禁止宣称 E2E**；统一为「全链路传输加密；P2P 直连时端到端加密」 |
| 设备 | 被控端 / 控制端 / 主机 混用 | **被控端（Host）** / **控制端（Client）** |
| 智能眼镜 | 笼统称「智能眼镜」 | 按品类区分：**显示型 / AI 型 / 一体机** |
| 指纹登录 | 「一键登录」「无感认证」「免密」混用 | 区分 **快捷登录（本地解锁）** 与 **免密登录**，二者安全等级不同 |

---

## 5. 优先级重构建议（P0–P3）

> 当前文档是「愿望清单」，无法排期。建议按下表重新组织。

| 优先级 | 范围 |
|---|---|
| **P0（MVP，可发布）** | Win / Mac / Android ↔ Win / Mac 互控 · 账号体系 + 实名 · P2P 直连（目标 ≥85%）+ 中继兜底 · **被控端**无人值守 + UAC + 开机自启 + 休眠唤醒 · 硬件编码 · 剪贴板 · 文件传输 · Web 控制端（临时码，免登录） · 生物识别**快捷登录**（非免密） · **全链路传输加密 + 连接模式指示**（禁止宣称 E2E） · 防滥用（二次确认 + 风控） |
| **P1** | 鸿蒙专项优化 · 多显示器 · 隐私屏 · 权限细分 · 实时语音 · iOS 控制端 · 弱网降级 |
| **P2** | 智能眼镜**桥接**（路径 B，仅显示型眼镜） |
| **P3（愿景，不做工程承诺）** | 智能眼镜**直连**（路径 A，标注硬件前提与触发条件） |
| **明确不做** | iOS 被控 · Linux 服务器运维级能力 · 远程打印（除非有明确依据） |

---

## 6. 待办清单（可勾选）

### 6.1 必做：修掉矛盾

- [ ] 1.1 延迟目标改为分场景 SLA，口径统一为端到端 P95
- [x] 1.2 加密章节重写：~~中继可见性~~ **已确认明文可见** ⇒ 禁止宣称 E2E，改「传输加密 + 直连时 E2E」，并补中继内控要求（不落盘 / 审批审计 / 留存策略）
- [x] 1.2b 🔴 **全产品文案排查**：官网 / 应用商店简介 / 隐私政策 / 投放素材中的「端到端加密」字样**全部替换** —— ✅ **已升级为 P0 发版阻塞项**（v2 §1「P0 发版阻塞项清单」A 组合规 1）；⚠️ **执行物（文案清单 + 上线前逐项核验）仍待产出**（属 D 组外部依赖，不写清单则阻塞项无法验收）
- [ ] 1.3 重写 5.1/5.2：设备识别 ≠ 身份认证，改「快捷登录」表述
- [x] 1.4 路径 A 降为长期愿景，标注硬件前提（DP Alt Mode / DeX）—— ✅ v2 §4 已标「**愿景**」、§7 Out of Scope 已列「智能眼镜直连（路径 A 工程承诺）不做」
- [ ] 1.4 明确智能眼镜品类定义（显示型 / AI 型 / 一体机）
- [x] 3.4 修正「键盘遮挡输入框」的因果描述 —— ✅ v2 已改为「软键盘弹出时视图被压缩」的准确因果
- [x] 3.5 修正「无感切换」为「自动切回，<1s」—— ✅ v2 已改（旧表述不可测量）

### 6.2 必做：补关键缺口

- [x] 2.1 新增「被控端需求」章节（无人值守 / UAC / 自启 / 唤醒 / 多显示器 / 硬编码 / 资源占用）—— ✅ **已建成独立章节（v2 §3）**，其中资源占用进一步细化为 **§3.7**
- [x] 2.1b 被控端资源占用已细化为独立章节：**v2 §3.7**（空闲态硬指标 / 会话中软指标 / 与用户争抢 / 资源档位 / 手机被控专项），并同步补 §5.2 五行指标与 §3.4 静止画面不得持续编码的红线
- [ ] 2.1c 🔴 **待实测回填**：被控端空闲态开销（任务管理器静置采样）与**手机被控 1h 息屏串流的耗电 / 温升 / 能否净充电** —— 后者直接决定获客钩子（差异化能力）是否成立（成本表 §10.1 #12 #13）
- [x] 2.1d 🔴 **主主张已定稿（D8）**：主战场 = 远程办公；主主张 = 「免费额度做到对手收费档位」；手机被控 = 获客钩子（非主主张）；已回写 v2 §0/§1/§3.6/§9.3、竞品表 §6/§7
- [x] 2.2 新增「成本模型」章节（打洞成功率 → 中继带宽 → 单用户成本 → 免费额度 → 付费墙）—— ✅ **已成独立文档**：`docs/成本测算表.md`（含 **§0 权威数值表 SSOT** + 公式 / 敏感性 / 打平 MAU 参数化 / 客服产能）
- [x] 2.2b 打洞成功率目标已定：**P0 ≥85%，优化后 ≥92%**，按 NAT 类型 / 运营商 / **网络环境**分桶统计（待实测锁定；分桶依据见 v2 §5.3）
- [x] 2.3 新增「合规与安全」章节（实名 / 防滥用 / 反诈 / 个保法 / 日志留存）—— ✅ 已补：v2 **§6.4**（剪贴板不上云 / 日志 6 个月与 `[D12-4]` 执行顺序 / 遥测边界）· **§6.5**（`[D12-3]` 延迟实名）；⚠️ **反诈与实名具体流程仍待法务定型**（已进 §10 台账「需法务」组）
- [x] 2.3b 中继明文可见的配套义务：内控审计流程 + 隐私政策明示告知（「P2P 失败时数据经服务器转发」）—— ✅ 审计与不可删/去标识规则见 `账号与管理系统设计` §5.5；隐私政策告知已进 **P0 发版阻塞项（A 合规）**
- [x] 2.3 标注 iOS 作为被控端：不支持
- [x] 2.4 新增「竞品对标表」，明确差异化论据
- [x] 2.4b 🔴 **竞品 DeX / 桌面模式适配已核实（2026-09-14）**：**无一家做「自动桌面化布局」**（TeamViewer **官方**称 DeX 只支持单向、被控端在 DeX 内是 feature request；RustDesk #14704 输入完全无法注入；ToDesk 仅手动横屏）⇒ 外接大屏由「⚠️ 待定」升为**次级差异点**，⚠️ 但 **D6 裁决不变不得当主主张**，且 Android 16（API 37）会逼对手跟进 ⇒ **窗口期 1–2 年**。详见 `竞品对标表.md` **§6.5** 与本文件下方「待核实项核实记录」
- [x] 2.4c 🔴 **控制端画面显示红线已定（D9，2026-09-14）**：**不得默认全屏** + **不得非等比拉伸**（等比缩放 + 黑边填充，**比例失真 = 0**）+ 不得擅自改被控端分辨率 / DPI —— 已新增 v2 **§2.4**，§0 / §1 / §5.2 同步
- [x] 2.4d 🔴 **PC→手机 已从 P2 提到 P0（D10，2026-09-14）**：「电脑帮父母看手机」是获客钩子的**主力方向**，且增量成本≈0（PC 控制端与 Android 被控端本就在 P0）⇒ v2 §1 / §9.3 / §3.6 / §5.2 同步；⚠️ **仍属钩子、非主主张**
- [x] 2.4e 🔴 **商业化与计费设计已出（2026-09-14）**：新增 **`docs/商业化与计费设计.md`** —— 回答「怎么收钱 + 技术上怎么控」。🔴 **三个关键结论**：① **付费墙只能挂中继**（直连技术上不可估量 ⇒ 与成本结论天然重合）② **内部计量用 GB、对外展示用分钟**（否则高画质用户静默击穿成本）③ **计费裁决必须在服务端**（客户端只能展示）。✅ **D11 已拍板（2026-09-14）**：5 项全部确认 ⇒ 免费版中继 **2 GB/月** · iOS **走 IAP** · 个人版 **¥158/年** · 流量包 **P1 再上** · 额度耗尽**允许「仅查看」**
- [x] 2.5 新增「账号生命周期」（找回 / 解绑 / 换机 / 设备上限）—— ✅ **已完成（2026-09-14）**：见 `账号与管理系统设计.md`（含注销 / 运营后台 / 个人中心三块），**已拍板 D12-1 ~ D12-6**（见 §2.5a）
- [x] 2.6 明确 Web 端能力边界（仅控制端 + 临时码模式）—— ✅ v2 已改「Web 端改临时码」（变更摘要「修正」行）
- [x] 2.7 补充：实时语音 / 弱网降级 / 自动更新 / 跨 NAT 兼容 / 包体积指标 —— ✅ **包体积已定量**（v2 §5.2：Win ≤ 100 MB / Android ≤ 60 MB / Mac ≤ 90 MB）· **跨 NAT 兼容**见 v2 §5.3 四档分桶；⚠️ 实时语音与自动更新策略仍待补（**不阻塞 P0**）

### 6.3 必做：结构重构

- [x] 手机独立远控独立成章（从第三部分拆出）—— ✅ 并入 v2 §2 核心体验，移动被控单独成节（§3.6）
- [x] 消除重复（一.1 vs 五.1；一.3 vs 二.1）—— ✅ v2 重构后已合并
- [ ] 新增「用户画像与核心场景」
- [x] 新增「非功能指标表」—— ✅ v2 §5.2（已从「大面积 `[待确认]`」改为 **P0 验收目标**）
- [x] 新增「Out of Scope」—— ✅ v2 §7
- [x] 全篇按 P0–P3 重排
- [x] 统一术语（见第 4 节）—— ✅ v2 §10 术语表（含 SSOT / 获客钩子 / 主主张双测试）

### 6.4 前置材料（缺了无法评估，建议先出）

- [x] 竞品对标表（功能 / 价格 / 延迟 / 差异点）—— ✅ `docs/竞品对标表.md`（含 §6.5 DeX 核实、§9 待核实 #1–#20）
- [x] 成本模型测算表 —— ✅ `docs/成本测算表.md`（含 **§0 SSOT 权威数值表**）
- [x] 非功能指标基线表 —— ✅ v2 **§5.2**（P0 验收目标化，含持有可读性 / 适配率 / 空闲态 / 功耗 / 包体积）

---

## 7. 已确认决策记录

| # | 决策项 | 结论 | 生效日期 |
|---|---|---|---|
| D1 | 中继是否可见明文 | **明文可见**（技术限制）⇒ **禁止对外宣称「端到端加密」**，改称「全链路传输加密；P2P 直连时端到端加密」 | 2026-09-14 |
| D2 | 是否设定打洞成功率目标 | **是**。目标：P0 ≥85%，优化后 ≥92%（按 NAT 类型 / 运营商分桶统计） | 2026-09-14 |
| D3 | ~~「手机为核心终端」~~ 的定位 | **修正**。手机不是「核心控制端」（控制端也可以是 PC；长时会话主力是 PC→PC）⇒ 正确定位：**PC 为被控主体，手机覆盖「PC 触达不到的时段」**（应急 / 触达 / 帮亲友）；~~对外主差异点改为「手机作为被控端」~~ ⇒ **被 D8 进一步修正**（手机被控只过差异化测试，不过权重测试） | 2026-09-14 |
| D4 | 手机端交互是否作为差异点 | **否**。移动端「不做桌面 UI 移植」是**及格线**，不写进宣传主张（交互可被对手 6–12 个月复制） | 2026-09-14 |
| D5 | 手机端「手持单屏」与「外接大屏」谁为主 | **手持单屏为主导路径**。理由：**身边无外接屏时，手机单屏是唯一可用的控制方式** ⇒ 它是**默认情况**，指标基线必须按它定；**外接显示器 / 电视为 P1 增强**（有则加分，无则不缺），**不当作差异点主张** | 2026-09-14 |
| D6 | 外接形态（外接显示器 + 键鼠）的权重 | **高**。在「手机能外接」与「不能外接」两种情况下**两者都做**（不是二选一）：不能接 ⇒ **手持必须保底可用**；能接 ⇒ **必须好用**。关键认识：**增量硬件仅为一块蓝牙键盘（~200 g）**（不是便携屏）⇒ 会话从**分钟级应急**跃升到**小时级轻办公** ⇒ **产生付费理由**。⚠️ **同时修正 v2 原排序**：外接**开发成本低（响应式布局，非新引擎）、ROI 最高，不应排最后**；手持交互引擎（新引擎）可**分期迭代** | 2026-09-14 |
| **D8** | **产品主战场与主主张到底是什么**（用户质疑：「手机应该是**控制端**吧？我们做的是远程控制电脑桌面吧？」） | 🔴 **质疑成立，且揭示一处方法错误**。**主战场 = 远程办公（远程控制电脑桌面）**：**PC→PC**（时长与收入主力）+ 手机 / 平板 →PC（碎片时段）。**主主张 = 商业条款**：「**免费额度做到对手收费档位**」（主战场内无技术护城河：D1 透明度弱于 RustDesk 自建 · D4 及格线 · D6 不可对外主张）。**手机作为被控端降为「差异化能力 + 获客钩子」**（⚠️ 非主主张）。⇐ **方法修正：主主张必须同时过两个测试**（**A 差异化**：对手 / PC 能不能做？**B 权重**：频次 × 付费意愿能否撑起收入？）—— D3 只验了 A。另：品类风险 —— 「远程桌面」在中文市场 ≈ 控制电脑，主打手机被控会被误认为手机远控软件。**P0 内手机被控降为第二优先级、不阻塞发版** | 2026-09-14 |
| **D9** | **控制端画面是否可以默认全屏 / 是否允许非等比拉伸**（用户要求：「电脑远程连接电脑，**不应该全屏或者让屏幕变形**」） | 🔴 **两层都要禁**。① **不得默认全屏** —— 连接后默认**窗口化自适应**，全屏仅由用户显式触发，退出全屏须恢复进入前的窗口尺寸与位置，断线重连不得改变全屏状态（理由：会毁掉「左边看远方桌面、右边做本地事」这一高频工作方式）。② **不得非等比拉伸** —— **必须等比缩放 + 黑边填充**（letterbox / pillarbox），**比例失真 = 0**（误差 < 0.5%）；非等比只在用户**主动**选择「拉伸铺满」时才允许且须标注会变形（理由：文字 / 图形失真 + 用户**误判远端控件位置与真实布局** ⇒ **工具可信度**问题，不是审美问题）。③ **不得擅自改被控端分辨率 / DPI 来「凑」窗口**（会打乱被控端用户的图标位置与布局，须经其同意且会话结束恢复原状）。补三种缩放模式（适应窗口 / 原始 1:1 / 拉伸铺满，默认适应窗口）与三个边界情况（竖屏窄条 / 超宽屏 / 高 DPI） | 2026-09-14 |
| **D10** | **「电脑帮父母看手机」= PC→手机 的优先级** | 🔴 **从 P2 提升到 P0，与 Android 被控端同批交付**。理由：① **增量成本≈0** —— PC 控制端与 Android 被控端本来都在 P0，PC→手机只是两者的**交叉**（被控端能力与控制端能力全部复用）② **否则钩子断在最有效路径上** —— D8 把手机被控定为「获客钩子」，前提是**子女这一端连得上**；而帮父母排障时**子女更可能用电脑**（大屏看清对方在点哪里 + 键盘打字快 + 可边查边做），排到 P2 等于把钩子的主力方向推迟两个版本 ③ §9.3 原「PC→手机 = 少见」写在 **D8 之前**，当时语境是「手机被控 = 主差异点」，现已过时（钩子成立**不依赖控制端形态**）。⚠️ **不因此升级对外主张** —— 仍属**获客钩子**，**非主主张**，D8 文案红线不变 | 2026-09-14 |
| **D11** | **商业化与计费设计：怎么收钱 + 技术上怎么控** | 🔴 **三个已确定的原则**：① **付费墙只能挂中继** —— 直连技术上**不可估量**（不经我方服务器）⇒ **「直连不限时长」是技术事实不是策略顾让**，且**直连侧不做任何计量埋点**（否则 D1 透明度资产会变成「你们在监控我」的负债）② **内部计量用 GB、对外展示用分钟**（按时长限额会被 2K60 用户静默击穿：同额度消耗、成本 **3 倍** —— 口径：2K60 = 12 Mbps vs 1080p30 = 4 Mbps，SSOT P5；旧写「8 倍」系基于已作废的 16 Mbps 假设）③ **计量裁决必须在服务端**（客户端计时可被改系统时间/篘改绕过；信令服务器测不到实际流量）。**另有 3 条机制红线**：④ 🔴 **额度必须能「中途被收走」**（短周期 ticket 续期 + 30s 计量心跳）—— 否则「用 1 分钟额度建立会话再挂 10 小时」⑤ 🔴 **额度耗尽四级降级**（预警 → 转直连 → 降码率 → 仅查看 → 才终止；**禁止直接黑屏断连、禁止静默降码率**）⑥ 🔴 **风控不得误伤「帮爸妈调手机」**（低频+异地+新设备 与「多账号薅额度」特征高度重叠 —— 否则风控会杀死唯一的获客钩子）。**已拍板 5 项**：**D11-1** 免费版中继 **2 GB/月**（⚠️ 待 TURN 报价重算）· **D11-2** iOS **走 IAP**（且**不上浮定价**）· **D11-3** 个人版 **¥158/年**（= ToDesk 同档，不得更高）· **D11-4** 消耗型流量包 **P1 再上**（P0 只做订阅 + 免费额度）· **D11-5** 额度耗尽**允许「仅查看」** | 2026-09-14 |
| **D12** | **账号与管理系统（运营后台 + 账号生命周期 + 个人中心）** | 🔑 **核心判断：P0 的「管理模块」不是后台，是 3 查 + 3 动作**（查用户 / 查额度与计量明细 / 查订单；调额度补偿 · 封禁解封加白 · 人工退款跳渠道后台）—— **完整后台是 P1，不一开始就造后台**。🔴 **两条铁律**：① 涉及资金与账号权限的操作**必须走工具 + 留审计，不得直接改库** ② **后台是已拍板政策的执行体**（D11 七天可退 / §5.6 主动补偿 / **§9.3 家庭互助白名单** —— 无工具则政策即空文）。🔴 **B（账号生命周期）优先级高于 A（后台）**：看不了后台只是我们难受，**解不了绑是用户用不了**。🔴 **两个最易错处**：① **「解绑」必须拆两种语义**（取消授权 vs 移除设备）否则旧机主仍可被连 ② **被控端必须一次点击断开且不得挽留**（隐私主体）。⚠️ 边界：**「我们的运营后台」≠「给企业客户的管理后台」**（v2 §7 排除的是后者）。**已拍板 6 项**：**D12-1** 个人版设备数 = 免费版 = **100 台**（不作付费权益；✅ **已于同日由 `[D15]` 修订为 150 台**）· **D12-2** 解绑后 7 天重绑需双验 + **90 天**未连接自动降级 · **D12-3** **延迟实名** · **D12-4** 注销 **30 天软删除** · **D12-5** 单次补偿 > 免费额度 10 倍需复签 · **D12-6** P0 只自研极小操作台 | 2026-09-14 |
| **D13** | **是否新增独立的「营销 / 增长模块」**（用户：「是否要增加一个营销模块？」） | 🔴 **不做独立模块**（v2 §7 明确列为**不做项**）。**最硬的理由：与 `[D12-6]`「P0 不造后台」直接冲突** —— 一个「能发东西出去」的营销模块（邀请返额度 / 优惠券 / 促销码）**必然**要求「发放 → 核销 → 对账 → 防作弊」后台。另三重理由：① **奖励正对我方唯一线性成本项** —— 竞品送授权**边际成本 = 0**，我方送中继流量**边际成本 = ¥0.30/GB 且随领取量线性增长**（`成本测算表` §1），且「多账号刷邀请」= **直接套现** ② **0 用户时是纯负债** —— 产品未上线、无转化基座可承接 ③ **已有零成本钩子尚未用尽** —— D8/D10 的「帮爸妈调手机」= **天然双端安装**，且**现金奖励会污染其可信度**（被帮的人会怀疑「他是为了拿奖励」）。⚠️ **同时澄清两类被误称「营销」的东西**：**投放 / ASO / 内容是运营动作**（非代码模块，不进产品范围）；**产品内推广位 / 活动中心 / 断连促销弹窗已被 §13.4 红线 1 与 5 禁止**。⇒ 改为在《商业化与计费设计》**§9.4** 写下**三条红线 + 一条边界**（奖励优先用零成本权益 / 发放须幂等+单账号上限+预算熔断+进审计 / 风控不得误伤钩子；渠道分成须单列） | 2026-09-14 |
| **D14** | **产品定价是否允许后续调整**（用户：「**产品定价不应该是固定价格，应该允许后续调价**」） | 🔴 **允许，且必须可调**（**不是固定价格**）。**但实现方式 = 商品与价格「版本化」，不是改那个数字** —— 指标：**调价的实现方式决定了它是资产还是事故**。**① 唯一纪律**：调价 = **新增一个版本**，已被订单引用的价格版本**只可新增、不可编辑**（订单是**财务凭证**；直接改价 ⇒ 历史订单「当时应付」无法复现 ⇒ 对账 / 退款 / 发票 / 税务 / 客诉全崩；与 §7.1 FEFO 额度账本**同一原理**）**② 价格与权益额度必须同版本原子快照**（只改价不改额度 = 毛利被静默侵蚀；只改额度不改价 = **卖一单亏一单**）**③ 四类场景分开支持**（新价对新人 / 新价含存量续费 / 限时活动价（**到期自动回落 + 提前提醒**）/ 渠道分价）**④ iOS 价源 = Apple**（App Store Connect 唯一价源 ⇒ 我方后台**只读镜像、不得编辑**，否则「后台 ¥18 / Apple 实扣 ¥24」口径分裂）**⑤ 五条红线**：不改已被订单引用的版本 · 不静默改存量续费价 · 不拆散「价格+权益」 · 不设无依据划线价 · 后台价 / 展示价 / 渠道实扣价 / 续费实扣价**必须同源**。**落地 = P1 完整运营后台**（与 `[D12-6]`「P0 不造后台」**不冲突**）⇒ 权限 = **管理员 + 双人复核 + 必填原因 + 预约生效**，**不得授予客服**（能改价 ⇒ 能自造折扣 ⇒ 内部套利）。⚠️ **`[D11-3]` 的价格上限不因「可调」而放松** —— 可调 ≠ 可涨，D14 主要服务于**降价 / 活动价 / 结构变化**。详见 `商业化与计费设计` **§2.6**；✅ **`[D14-1]`~`[D14-4]` 四子项已全部拍板**（2026-09-14，用户采纳建议默认：涨价不对存量迁移 / 降价自动惠及 + 告知 / 活动价自动回落 + 提前 5 日提醒 / iOS 价源 = Apple 只读镜像） | 2026-09-14 |
| **D15** | **设备数：维持 100 台，还是两档都提到 150 台？**（`需求规划评审意见` §10.5 #1；用户：「按你的建议来」） | 🔴 **采纳 B 案：两档都提到 150 台**（修订 `[D12-1]` 的 100 台）。**理由**：① **ToDesk 专业版 = 150 台 @ 同价 ¥158/年** ⇒ 原「**同价位显劣**」会被用户截图打脸（在定价页当场看得到）② **设备数边际成本 ≈ 0**（不连就不花中继流量；占配额 ≠ 消耗带宽）⇒ 这是**极少数可以零成本堆的免费权益** ③ **为主主张新增一条可验证事实**：「**对手付费档位的设备数，我们免费版就给**」（配合「对手收费 ¥25/月的手机被控，我们免费」）④ **消掉比价劣势** —— 原方案在「同价」前提下比对手少 50 台，属**无谓失分**。🔴 **不变量：仍不作付费权益**（两档相同 ⇒ 权益对比表写成「免费版优势行」）· ❌ **不得写成「设备数是我们优势」、不得当主标语**（用户不会为设备数付费）。⚠️ **需回答的问题**：「为何是 150 而不是 100」= **对齐对手付费档、消除比价劣势**。⚠️ **连带影响**：上限抬高 ⇒ **闲置设备更易堆积** ⇒ `[D12-2]` 的 90 天降级与「降级即释放配额」**从「应有」变成「必须」**。⇒ SSOT `成本测算表` §0 **P15**；路线对比见 `竞品对标表` **§2.5a** | 2026-09-14 |
| **D16** | **日志留存期限**（`[D12-4]` 关联的前置依赖；用户「按你的建议来」） | ✅ **6 个月**（到期**自动删除**）。🔴 **三条硬条件**（缺一即不可落地）：① 保留期**不得短于** `[D12-4]` 的 **30 天冷静期** ② 注销后**必须可匿名化**（身份数据期满级联删除、计量与审计流水**去标识保留**）③ 到期删除**必须是自动任务**（不得依赖人工）。**执行顺序**：先判定是否在 30 天冷静期内 → 期内**只标记不删**（可撤销注销）→ 期满**级联删除 + 匿名化**。⇒ **解除了 D12 的「注销流程前置依赖」**（`成本测算表` §10.1 **#16 已结案**）；⚠️ 成本上使日志存量 **×6**，但日志属**元数据（≈0 量级）**，**不改变成本结论**；反写 v2 §6.4 · E6 **A3** · `账号与管理系统设计` §2.7 / §6 | 2026-09-14 |
| **D17** | **反诈 / 实名具体流程**（`[D12-3]` 延迟实名的落地路径；用户「按你的建议来」） | ✅ **采「可验收的默认路径」** —— **不等人**（法务回复周期不可控），先把产品结构与验收口径定下来。**三类触发事件**（命中任一 ⇒ 要求实名）：① **触发风控**（短时大量连接 / 异地跳变 / 被举报）⇒ 拦截 + 实名入口；未实名降级为「仅可直连、可用时长受限」② **使用中继额度**（超阈值）⇒ 弹实名引导；未实名则中继不可用（**直连 / 内网不受影响，保基本可用**）③ **跨账号连接** ⇒ 被控端二次确认 + 可选实名；异常连接直接拦截。**其余 P0 必占位**：实名入口 · 未实名降级路径 · **被控端首次二次确认**（含反诈文案，**不得折叠隐藏**）· 异常连接拦截。🔴 **P0 验收口径 = 「触发链路与入口可用」，不是「人人先实名」**（后者会直接损失注册转化）。⚠️ **保留「最终文案与口径须法务复核」标记**（法务 3 问：实名数据留存边界 / 内容安全处置义务边界 / 资质判定）⇒ **不阻塞开发，属 P0 发版阻塞项**（v2 §1 E6 新增 **A8**）；反写 v2 **§6.5** · `账号与管理系统设计` **§2.6** | 2026-09-14 |
| **D18** | **TURN 报价 / 成本参数能否后台管理、后续可调？**（用户：「这个报价不能通过管理后台管理起来吗？后续可以调整？」） | 🔴 **是，且必须。** 先分清两类「报价」：**(A) 输入成本**（TURN 单价 $P$ / 方向系数 $k$ / 保底带宽）= **采购合同参数，我方只能录入收到的报价，不能「管理」出一个报价**；**(B) 由 (A) 派生的产品参数**（免费额度 / 码率上限 / 降级阈值 / 对外「分钟」折算）= **必须后台可配、可版本化、可热调**。⇒ 🔑 **`[D18]` 不是新理念，是 `[D14]` 的镜像补全**：`[D14]` 让**售价**可调，`[D18]` 让**成本**可调 —— **缺了后者，前者就是盲调**（只知售价、不知成本）。落地 = **两层模型**（`cost_param` 人填版本化 + `derived_metric` 系统算，🔴 **推导层禁止手填**）；时点 = **P0 定模型与入口、P1 出界面**（随 `账号与管理系统设计` §5.3，与 `[D12-6]`「P0 不造后台」不冲突）。🔴 **五个不能简化成一个数字框的点**：① 单价是**阶梯**（四档跨度 5.3 倍）不是单值 ⇒ 必须**先选档位语义再覆盖单价**，否则报价到手全环境返工 ② $k$ 是**开关**不是数字（翻倍效应）③ 保底带宽是**固定成本科目**（不得与按量单价同框）④ 权限比定价更严 ⑤ 必须记**报价来源**。🔴 **`[D18]` 不能替代询价本身** —— 二者正交：**阻塞的本质 = 不知道该填什么**（只能问厂商），`[D18]` 解决的是**改起来多快** ⇒ 收益是把元阻塞**降级**为「**阻塞对外公布数值**」（从「阻塞产品设计与开发」降为「阻塞对外口径」）。⚠️ **诚实边界**：$k$ **不是可调参数，是商务条款**（只能问出来），且 $k$=2 ⇒ 成本翻倍 ⇒ `[D11-1]`（2 GB）**除以 2.7** ⇒ **免费版主主张被击穿**（结构性后果，不是「填个数」）。⇒ $k$ **强制显式、无默认值**（未填 ⇒ 系统拒绝对外额度输出）。详见 `成本测算表` **§3.4** · `商业化与计费设计` **§2.7** · `账号与管理系统设计` §5.3 / §5.4 | 2026-09-14 |
| **D19** | **是否支持远程看视频 / 打游戏 / 听音乐？微信在远程能否远程接听 / 语音 / 视频？**（用户原问）+ 「**刚才说的功能很重要，也是亮点**」（用户纠偏） | 🔴 **先纠一个判断错**：首轮把它当「成本重的可选项」（风险优先）—— **错**。正确姿势：**先判断是否刚需，是刚需就直接进「怎么安全地做」**。🔑 **它确实是刚需，而且比「刚需」更重：是 `[D8]` 钩子的另一半** —— 「帮爸妈看手机」只解决**看到**，不解决**说清** ⇒ 排障一半时间花在「你点那个」「哪个？」的来回转述上 ⇒ **只看不听 = 钩子只完成一半**。且**成本几乎为零**（128 kbps ≈ 4 Mbps 的 3% ⇒ ¥0.02/中继小时）⇒ **不是成本问题，是及格线问题**。**`[D19-1]` 范围** = ✅ **P0 补三合一音频通道**（① 系统音频回传 Win WASAPI loopback / macOS 13+ ScreenCaptureKit / Android `AudioPlaybackCapture`；② 语音对讲，**播放不需驱动**；③ 纯音频模式）。**`[D19-2]` 新战场** = ✅ 由「远程办公」扩出「**远程陪伴 / 远程照料**」（与帮爸妈同源），⚠️ **但不改变 D8 主主张与品类定义**。**`[D19-3]` 微信口径** = ✅ 支持「**边看边说**」（自建语音通道）；⚠️ **虚拟麦克风 / 摄像头注入降为 P1**（需签名内核驱动；🔴 **用被控端真麦克风 ≠ 注入** —— 那传的是**被控端现场物理声音**，方向相反）；❌ **不承诺** 安卓通话音频 / DRM / iOS 被控 / 竞技网游。**`[D19-4]` 影音游戏** = ✅ **P1 新增「高帧率影音档」**（2K60 / 4K60 + 音频增强，主打**影音 + 单机 / 策略 / 卡牌**）—— 🔴 **这可能是 D11 缺的那台「ARPPU 引擎」**（D11 说「必须做增值插件把 ARPPU 推到 ¥25」但**从未定义插件**）；🔴 **反作弊风险必须在购买页明示**。**`[D19-5]` 计费** = ✅ 音频**计入会话总量、不单开科目**（实占中继，仅 3%）；🔴 **纯音频模式必须单列更低口径**（否则「只听声音」与「看 1080p30」扣同样多，而成本差 **40 倍** —— 这是「不得对最贵通道免单」的**反向同款：不得对最便宜用法按最贵用法收费**）。**`[D19-6]` 验收** = ✅ 音画同步（滞后 ≤ 125 ms / 超前 ≤ 45 ms `[需核实]`）· 音频断连不中断画面 · 被控端静音即时生效。🔴 **四条硬边界（不是「不做」，是物理 / 法律不可能）**：① Android 通话音频（`AudioPlaybackCapture` 仅 `MEDIA`/`GAME`/`UNKNOWN`）② DRM 内容（HDCP / PlayReady / Widevine，**绕开违法**）③ iOS 作被控端 = 0 可能 ④ 竞技网游（输入注入被判宏 / 外挂 ⇒ 封号）。🔴 **隐私红线**：**被控端不得被静默采音** —— 必须**可见提示 + 一键静音**（采系统混音会连私人通话一起传出）。详见 v2 **§0 / §1 P0 / §1 P1 / §4.4 / §5.2 / §7** · `成本测算表` §0 **P16 P17** / **§3.2.1** / §3.3 / §10.1 **#20 #21** / §11 · `商业化与计费设计` §1 **L4b** / §1.1 / §2.2 / **§5.7** / §13.2 **#9** / **§13.4 红线 6** / §12 · `竞品对标表` **§6 / §6.6** | 2026-09-14 |
| **D26** | **应用外壳层（托盘 / 通知区）与安卓被控端的界面归属**（用户：「我们是否有对桌面端的托盘图标、桌面图标、载入动态页面进行 UI 设计？对移动端的桌面图标、载入页？Web 端的地址栏图标？」） | 🔴 **审计结论：四类资产里三类无帧**。① **托盘 / 通知区图标 + 右键菜单**：此前**只有文字规格**（v2 §3.3 开机自启两链路 / §3.7 常驻进程 / §1 红线「常驻 ≤ 3」），全稿无帧 ⇒ **本轮补 2 帧**（`W1-12` 控制端托盘 · `W2-09` 被控端托盘）② **加载 / 失败 / 不可达三态**：控制端侧已有，**被控端侧缺** ⇒ **补 1 帧**（`W2-10` 启动 / 初始化失败态；⚠️ 只部分关闭 README §11.5.2 的 `R8`）③ 🔴 **安卓被控端单开一页**（`W7`，6 帧全 P0）—— v2 §3.6 原只有**技术路径文字、无任何界面证据**，而「逐项授权 / 前台服务通知 / ROM 省电」三处差异**不可复用 PC 语义的 `W2`**，且**授权链本身就是安全底线的执行体** ④ **桌面图标 / favicon / 启动闪屏仍不出稿**（属视觉阶段资产：灰方块不产生决策信息）⇒ 但**冻住两条规则**：托盘图标必须有「会话中」可见区分、**Android 前台服务通知文案必须在需求阶段定稿**（系统模板渲染，无法后期美化）。🔴 **三条红线**：托盘**必须**能一键断开且**与「退出」不得合并** · 常驻进程仍 **≤ 3**（新组件不得各自注册托盘图标）· 安卓被控端全页**不得出现「额度」与任何金额 / 催费 / 推销**。⇒ 新增线框 `w7-安卓被控端.html`（新页）+ `W1-12` / `W2-09` / `W2-10`；回写 v2 **§3.6**（新增「界面口径」表）· README **§2.1 / §2.2 / §11.10** | 2026-09-15 |
| **D27** | **跨帧一致性收口**（用户：「**C · 收口高危缺口**」；`README` §11.11.5 的「方案 C」） | 🔴 **本轮不补新功能，只收前几轮留下的跨帧裂缝**。**七帧全 P0、全层①**：`W1-13` 连接失败 / 超时 · `W1-14` 用尽后的四条路 · `W2-11` 音频三开关事前默认值 · `W3-11` 核弹动作统一二次确认组件 · `W3-12` 账号表单错误态总表 · `W4-10e` 支付结果态 E（查询用尽 → 人工对账）· `W7-07` 收到连接请求。**收的三类缺口**：① **失败态 / 错误态缺失**（`R24` / `S5`）—— 转化链在「失败」处断掉，用户看不到「谁的问题 / 已为你做了什么 / 下一步能点什么」② 🔴 **危险动作的按钮权重低于常规动作**（`R19` / `R30`）—— 「退出所有设备」「提交注销」原用 `.btn.sm`，比「移除设备」还弱 ③ **同一概念多处各写一遍**（`R20`）—— 「不操作的观看态」在稿里有 4 种写法。**两条强制约定（本轮的真正产物）**：**① 按钮权重** ⇒ 核弹动作**不得低于常规动作权重**、必须复用**同一**二次确认组件、触发按钮必须带 `data-confirm="<动作名>"`，名单落成 README **§6.3 危险动作台账**；🔴 **禁止**用「输入留言 / 联系客服 / 延迟 N 天 / 转成邮件发给我」充当二次确认。**② 术语单一源** ⇒ 规范词 = **仅查看**（凭「UI 面使用频次 54:5」选多数派，而非凭声明）；**只读（模式义）/ 观察模式 / 纯观察模式 / 仅观看** 为 UI 面禁止词。🔴 **两条约定均已成机器可断言的规则**（门禁 `C17` / `C16`，`C17` 与 §6.3 台账**双向对拍**），并就地修掉 5 处（`W2-08` 麦克风自相矛盾归一 · `W3-09` 权重过低 · `W3-06` 补 `data-confirm` · `W7-02` 明写总开关 · 跨帧引用帧号订正）。**计数影响**：界面条目 **68 → 75**（① 50 → 57）· 线框图 **74 → 81** · 加高帧 44 → 46。⇒ 详见 README **§6.3 / §11.12** | 2026-09-15 |
| **D28** | **残项收口（第十一轮）**（用户：「继续」；`README` §11.11.5 的剩余 6 项） | 🔴 **只收台账，不补新功能**：把第九轮评审（README §11.11.2）留下的 **6 项残项**清空 —— **R16 / R23**（规格块混 UI）· **R24 的 W5 侧**（零表单错误态）· **R27**（条件分支并列排版）· **R28**（无横屏帧）· **R29**（三份重复的权限说明）· **R31**（托盘与主开关状态同步未定义）。**产物两类**：① **补 2 帧（均 P0）** —— `W5-09` **表单错误态总表**（实名 6 类 + 运营台登录 2 类，**新条目 33d**；🔴 就地呈现 · 保住已填 · **系统故障 ≠ 用户错误** · **不得泄露账号是否存在** · **不得暗示绕过实名**）· `W6-09a` 沉浸式手持**横屏子态**（命中区随旋转重排，属条目 44 的第二态、**不另占编号**）② **两条强制约定（机器可断言）** —— **① 规格块（`.spec`）只能落在画布外**（门禁 `C18-a` 画布内无 `.spec` 块 / `C18-b` 画布内无「规格 / 给开发 / 实现说明 / 验收点 / 设计意图」等词；`wireframe-scan.mjs` 的可见文案剥离同步跳过 `.spec`）· **② 同一件事的多处呈现必须指定唯一源**（`W5-06` 权限块改为**一张矩阵 = 唯一来源**，三个动作框不再重复权限结论；`W2-09` 托盘「可被连接」定为**状态回显**，**不得**出现第二个同类开关）。**就地修复 5 处**：`W1-04` / `W3-06` / `W4-02` / `W5-07` 的开发向文字迁出画布（画布内不得再出现「规格 / 验收点」类词）· `W7-06` 三档改**互斥**口径且「支持」与「不支持」**不再并列** · `W2-09` 托盘与主界面归一为同一状态。⚠️ **本轮自查中 `W5-06` 一度引入 198px 静默裁切**（权限表 287 → 604px），已当场收窄表头（`72/66/86/88/66`）+ 缩短备注文案**回退**，并用几何门禁复核（该溢出**本轮引入、本轮回退**）。**计数影响**：界面条目 **75 → 76**（① 57 → 58 · P0 67 → 68）· 线框图 **81 → 83**（自由画布 4 → 5 · 标准默认 31 → 32 · 加高帧 46 不变）。⇒ 详见 README **§11.13** / **§12.2**（新增门禁 `C18`） | 2026-09-15 |

> ⚠️ **D6 的叙事边界（易被推错）**：外接形态**不得对外当主主张**（DP Alt Mode 非全线 + 依现场是否有屏）⇒ **对内高优先、对外只作能力说明**；
> 且**不能宣称「以后出差不用带笔记本」**（依赖现场有屏 + 网络能穿透 + 不热降频 三变量同时成立）⇒ 正确叙事是 **「应急与轻办公」**。

### D8 的连带影响（已回写 v2）
### 🔴 待核实项核实记录：竞品 DeX / 桌面模式适配（2026-09-14）
### D1 的连带影响（须一并处理）
### D2 的连带影响
### D3 的连带影响（已回写 v2）
### D5 的连带影响（已回写 v2）
### D6 的连带影响（已回写 v2）
### D9 的连带影响（已回写 v2）
### D10 的连带影响（已回写 v2）
### ✅ D11（已拍板 2026-09-14）：商业化与计费设计


## 8. 下一步

已同步产出重构版本：`docs/需求规划-v2-P0P3重构.md`（按 P0–P3 组织，标注了继承原文 / 新增 / 待确认）。

✅ **D11 已拍板（2026-09-14）**：`docs/商业化与计费设计.md` §11 的 5 项已全部确认（**免费版中继 2 GB/月 · iOS 走 IAP · 个人版 ¥158/年 · 流量包 P1 再上 · 额度耗尽允许「仅查看」**）⇒ 已回写 v2 §8、`成本测算表.md` §7.2 / §7.5 / §11、`竞品对标表.md` 附速查。⚠️ **D11-1 的 2 GB 是「待报价复核」的临时值** —— 拿到 TURN 报价后需复核。

✅ **D12 已拍板（2026-09-14）**：`docs/账号与管理系统设计.md` §7 的 6 项已全部确认（**D12-1** 个人版设备数 = 免费版 = 150 台（✅ **原 100 台已于同日由 `[D15]` 上调**；不作付费权益）· **D12-2** 解绑后 7 天重绑需双验 + 90 天未连接自动降级 · **D12-3** 延迟实名 · **D12-4** 注销 30 天软删除 · **D12-5** 单次补偿 > 免费额度 10 倍需复签 · **D12-6** P0 只自研极小操作台）⇒ 已回写本文 §2.5 / §2.5a / §7、v2 §1 / §6.2 / 变更摘要、`商业化与计费设计.md` §13.3、`竞品对标表.md` §9 **#20**。
✅ **D12-1 的连带待核实已闭环**：对手付费档设备数已核实 —— **ToDesk 专业版 = 150 台**（同价）⇒ 已由 **`[D15]`** 处置（我方**两档均 150 台**，`竞品对标表` §9 **#20** / §2.5a 已结案；残留仅向日葵 / AnyDesk，**已不阻塞任何措辞**）。
✅ **D12 的前置依赖已解除**：账号注销流程依赖的「日志留存期限」已由 **`[D16]`** 拍板 = **6 个月**（`成本测算表` §10.1 **#16 已结案**）⇒ 注销级联**可定稿**。

✅ **D13 已确定（2026-09-14）**：**不做独立的「营销 / 增长模块」**（`需求规划-v2-P0P3重构.md` §7 已列为明确不做项）。🔴 **理由中最硬的一条：它与 D12-6「P0 不造后台」直接冲突** —— 任何「能发东西出去」的营销模块必然要求「发放 → 核销 → 对账 → 防作弊」后台。另：**奖励正对我方唯一线性成本项**（竞品送授权**边际成本 0**；我方送中继流量 **¥0.30/GB 且线性** ⇒ 「多账号刷邀请」= 直接套现）；且**已有零成本钩子（D8/D10 天然双端安装）尚未用尽**，现金奖励反而会污染家庭互助场景的可信度。
⇒ 改为在 `商业化与计费设计.md` **§9.4** 写下**三条红线 + 一条边界**（奖励优先用边际成本≈0 的权益 · 发放须幂等 + 单账号上限 + 预算熔断 + 进审计 · 风控不得误伤钩子；渠道分成须单列）；`成本测算表.md` §10.1 新增 **#17 渠道分成 / 分销佣金比例**。
❓ **若你所说的是产品内的推广位 / 活动中心**：该方向**已被 §13.4 红线 1 与 5 禁止**（不做断连促销弹窗、技术状态与商业计数不得混为一体），不是「没规划」而是**已明确否掉**。

✅ **D27 已拍板（2026-09-15）**：**跨帧一致性收口**（README §11.11.5 的「方案 C」）⇒ 补 **7 帧全 P0**（`W1-13` / `W1-14` / `W2-11` / `W3-11` / `W3-12` / `W4-10e` / `W7-07`），并把两条约定写成**门禁可断言**的规则：**① 危险动作按钮权重**（README **§6.3 台账** + 门禁 `C17` / `C17-b`）· **② 术语单一源「仅查看」**（门禁 `C16`）。⚠️ **仍留的历史裂缝**（不因本轮而消失）：`R21`（`W2-02` 不显示控制方身份 · 且其 `note②`「拒绝与允许并列」与本轮「危险动作不得弱化」口径**相互冲突，待裁决**）· `R23`（规格块不得混 UI ⇒ 改 `.spec`）· `R24` 的 **W5 侧零错误态** · `R25` 的 `W4-09` 侧「还有三条免费路」· `R26` 的 `W7-02` 标记本体可点 · `R27` / `R28` / `R29` / `R31`。⇒ 台账见 README §11.11.2 / §11.12。

✅ **D28 已拍板（2026-09-15）**：**残项收口（第十一轮）** ⇒ 第九轮台账（README §11.11.2）的**最后 6 项**（`R16` / `R23` / `R24` 的 W5 侧 / `R27` / `R28` / `R29` / `R31`）**全部收口** ⇒ `R19`–`R32` **全清**（其中 `R21` / `R25` / `R26` 已由 `[D27]` 先行收口，见 README §11.12）。**产物**：**补 2 帧全 P0**（`W5-09` **表单错误态总表** = 新条目 **33d**、也是**全稿表单错误文案的单一源**；`W6-09a` 沉浸式手持**横屏子态**）+ **两条机器可断言约定**（`.spec` **不得进画布**，门禁 `C18-a`/`C18-b`；**同一件事只允许一个源**）。⚠️ **残留一项待裁决**：`W2-02` 的 `note②`「拒绝与允许并列」与「危险动作不得弱化」口径**相互冲突**（D27 评审提出）⇒ 下一轮裁决。⇒ 台账见 README **§11.13**。

---

## 9. 本轮全量评审（2026-09-14 · 业务专家复评）的登记与落地

> 📦 **本小节已外置（历史记录，默认不读）** → [`评审意见-全量评审登记.md`](archive/评审意见-全量评审登记.md)
> 🔴 **仍活跃的台账在正文 §10**（待确认总台账：待填参数 / 需询价 / 需实测 / 需核实）。
### 9.1 🔴 A 组：6 条硬矛盾（必须修，已修）
### 9.2 🟡 B 组：10 条口径 / 体例（不修会出现「看起来矛盾」的引用错误，已修）
### 9.3 🟠 完善缺口 10 项（本轮补上）
### 9.4 🟢 增强 E1–E7（新增章节 / 交付物，已落盘）


## 10. 🔴 待确认总台账（跨文档汇总 · 拍板前不得对外用数）

> **用法**：这是**唯一**的「现在还卡在哪」入口。凡在本文件、`成本测算表`、`商业化与计费设计`、`账号与管理系统设计`、`竞品对标表` 中出现的 `[需实测]` / `[需询价]` / `[需核实]` / `[待确认]`，**都在这里归组**。
> 🔴 **纪律**：① **待填参数（TURN 报价，`[D18]` 降级）未到手前，全部额度与定价数值只能作为假设**，对外材料**不得出现具体数字**；② 竞品侧数字**必须带抓取日期**，未亲验一律 `[需核实]`。

### 10.1 🔴 待填参数（1 项 · `[D18]` 已降级：**阻塞对外口径，不阻塞开发**）

> 🔴 **`[D18]` 口径修正（2026-09-14）**：本项原标「**元阻塞**」，现改为「**待填参数**」。
> 理由：① 产品侧数值（免费额度 / 码率上限 / 展示折算）**全部后端下发、不硬编码** ⇒ **未知的只是「该填什么」，不是「能不能建」**
> ② 成本参数台账（`成本测算表` §3.4）**可以先建**，报价到手后**一次录入即自动重算下游**，**不必改代码 / 改文档**
> ⇒ **它阻塞的是「对外公布数值」，不阻塞产品设计与开发。**

| 项 | 内容 | 影响 | 位置 |
|---|---|---|---|
| **TURN 报价** | ① **入方向流量是否计费**（$k$ = 1 或 2）② 中继流量单价 $P$（目标 ¥0.20–0.30，按量 / 流量包 / 年度承诺三档） | 🔴 **决定全部额度与定价数值减半与否**；$P$ 从 ¥0.30 → ¥0.80 则 `[D11-1]`（2 GB）**除以 2.7** | `成本测算表` §10.2 **#1 #2** · 台账机制见 §3.4 · 标签见 §11 末行 |

### 10.2 `[需询价]`（8 项 · 云厂商 / 商务）

| 项 | 关键问题 | 位置 |
|---|---|---|
| 保底独享带宽价格与超量计费 | 固定成本最大项 | `成本测算表` §10.2 #3 |
| 弹性带宽扩容时延 | 影响保底容量设定 | #4 |
| 境外节点单价 | 若做「全球节点」增值包 | #5 |
| 短信 / 实名接口单价与阶梯 | ¥0.03–0.05 / ¥0.5–1.5 的验证 | #6 |
| 🔴 各支付渠道费率 | 微信 / 支付宝 0.6% · **iOS IAP 30%（15%）** · 国内安卓商店分成 | #7 · SSOT P13 |
| 🔴 App Store 3.1.1 合规边界 | 「站外支付引导」的落地边界（文案 / 链接 / **客服话术** / 帮助文档） | #8（另见 `商业化与计费设计` §13.4 红线 4） |
| 🔴 App Store 小企业计划 | 申请条件与生效时点（决定 iOS 分成 30% → 15%） | #8 |
| 渠道分成 / 分销佣金 | 🔴 **有真实现金成本，须单列进 CAC**（与「零成本权益」不是一回事） | `成本测算表` §10.1 **#17** · `商业化与计费设计` §9.4 |

### 10.3 `[需实测]`（21 项 · 内部埋点 / Beta 数据）

完整表见 **`成本测算表` §10.1 #1–#21**。其中**阻塞发版或阻塞决策**的 8 项优先做：

| 优先 | 项 | 为什么它是优先项 |
|---|---|---|
| 🔴 P0 | #14 **2K / 4K 实际码率** | 定价直接依赖（2K60 若 16 Mbps ⇒ 10 GB 时长折半） |
| 🔴 P0 | #3 **打洞成功率（总体 + 分桶）** | `[D2]` 看板本体；也是免费额度与带宽预算的自变量 |
| 🔴 P0 | #1 **实际编码码率**（桌面 / 视频 / 静止） | 全部单位成本的分母 |
| 🔴 P0 | #18 **客服工单率与首响 / 解决时长** | 决定 §6.3 人力配比 ⇒ 决定打平 MAU |
| ⚠️ P0 | #12 **被控端空闲态开销**（不得有恒定 CPU 基线） | `[D1]`/`[D9]` 之外唯一会直接毁掉口碑的硬指标 |
| ⚠️ P0 | #13 **手机被控功耗 / 温升 / 能否净充电** | 决定**获客钩子是否成立** |
| 🔴 P0 | #20 **音频三通道实际码率 + 纯音频模式时长分布**（`[D19]`） | 验证「纯音频模式单列更低口径」是否够低（`商业化与计费设计` §5.7）；也用于确认音频占比确实是 ≈3% |
| 🔴 P0 | #21 **通话类音频能否采集**（`[D19]` 红线） | 决定「不支持」能否写成**有依据的对外口径** —— 而不是含糊承诺（安卓微信语音 / 视频通话、各端 loopback 结果） |

### 10.4 `[需核实]`（竞品与对客口径）

| 项 | 内容 | 用途 | 位置 |
|---|---|---|---|
| 对手是否对 **P2P 直连**计时 | 若对手都计时而我们不计 ⇒ 「**直连真的不消耗额度**」升级为主题词 | 对外口径防守 | `竞品对标表` §9 **#14** · `商业化与计费设计` §12.1 |
| 对手是否**展示当前连接方式**（直连 / 中继） | 决定我方「连接模式指示器」是**差异点**还是**及格线** | 决定第 ① 类界面的投入 | `竞品对标表` §9 **#16** · `商业化与计费设计` §13.6 |
| 对手额度耗尽的界面处理 | 确认「四级降级 / 不弹促销窗」是空位还是及格线 | 界面设计基线 | `竞品对标表` §9 **#17** |
| 🔴 **各厂商付费档设备数上限** | **ToDesk 已核实：专业版 = 150 台** ⇒ ✅ **已结案（`[D15]`，2026-09-14）：我方两档均提到 150 台** ⇒ 原「同价位显劣」**已消除** | 权益对比表措辞（`竞品对标表` **§2.5a**） | `竞品对标表` **§2.5** · §9 **#20** |
| 向日葵 / AnyDesk 付费档设备数 | 同上（残留未取到）—— ⚠️ **已不阻塞任何措辞**（我方已对齐 ToDesk 专业档），仅作竞争情报 | 同上 | `竞品对标表` §2.5 残留项 |
| 国密 SM2/SM3/SM4 路线 | 向日葵已有国密 + VPN 许可；我方**缺失**（列 P1） | 政企 / 投标场景的前置门槛 | `竞品对标表` §8 · v2 §6.1 |

### 10.5 ❓ 需用户 / 法务拍板（当前 0 项 · ✅ 已清空）

> ✅ **已拍板迁出（两批）**：
> ① 原 #4「`[D14]` 调价策略四问」已于 **2026-09-14** 拍板（用户采纳建议默认）⇒ 登记在 §7 决策表 **D14-1 ~ D14-4**，落地细则见 `商业化与计费设计` **§2.6.3**。
> ② 原 **#1 / #2 / #3** 已于 **2026-09-14** 同日拍板（用户：「按你的建议来」）⇒ 登记为 **`[D15]` 设备数 = 两档都 150 台 · `[D16]` 日志留存 = 6 个月 · `[D17]` 反诈 / 实名 = 可验收的默认路径**（详见 §7 决策表）。
> 🔴 **唯一仍属「外部阻塞」的只有法务 3 问**（实名数据留存边界 / 内容安全处置义务边界 / 资质判定）—— 已由 `[D17]` 编入 **v2 §1 E6 清单（A6 / A8）**，**不阻塞开发，但阻塞发版**。

| # | 待拍板项 | 结论 | 状态 |
|---|---|---|---|
| ~~1~~ | ~~设备数：维持 100 台，还是两档都提到 150 台？~~ | ✅ **`[D15]`：两档都 150 台**（边际成本 ≈ 0；消掉「同价位更少」；仍不作付费权益；不得当主标语） | 已拍板 |
| ~~2~~ | ~~日志留存期限（`[D12-4]` 关联）~~ | ✅ **`[D16]`：6 个月**（≥ 30 天冷静期；注销后可匿名化；到期自动删除） | 已拍板 |
| ~~3~~ | ~~反诈 / 实名具体流程（`[D12-3]` 延迟实名）~~ | ✅ **`[D17]`：三类触发事件 + 未实名降级路径 + 被控端二次确认 + 反诈提示位**；⚠️ **最终文案仍留法务复核** | 已拍板（留法务标记） |

> 🔴 **两项对外纪律（2026-09-14 更新）**：
> ① **在 TURN 报价到手前**，任何对外材料**不得出现**具体额度数字（2 GB / 62 min）与具体打平 MAU —— ⚠️ **仍然生效**（`[D18]` 已降级为「待填参数」，但**仍阻塞对外口径**）；
> ② ✅ **设备数纪律已解除**（`[D15]` 已拍板）⇒ **可以正面披露设备数**，🔴 **但必须按 §2.5a 口径**：**并列写出两家数值**（ToDesk 专业版 150 / 我方 150）、**不得作付费权益**（写成「免费版优势行」）、**不得当主标语**（只能与「手机被控免费」「直连不限时长」两条真优势同屏作防守行）。

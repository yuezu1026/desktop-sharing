# S4 · 愿景，不排期

> 对应需求 §1 的 P3。本文件存在，是为了防止有人把它排进 MVP 或 S2。

## 0. 现在不做什么

- 眼镜自己安装客户端、自己打洞、自己当控制端或被控端。
- 镜片里的通知、日历、用语音打开任意应用。
- 为「以后也许有这种眼镜」预留接口、预写协议或预占人力。

## 1. 什么情况下才重新打开

消费级眼镜同时满足：机身能独立运行、系统允许第三方客户端、不依赖某家私有通道。三个条件缺一个，本阶段继续关闭。

重新打开时单独立项，不从 S3 顺延加功能。S3 的桥接做完，也不等于 S4 开始。
